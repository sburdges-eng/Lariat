**Avatar** — a round initials chip in the display grotesque on amber. The cook / staff mark.

```jsx
<Avatar name="Rosa Mendez" />
<Avatar initials="JT" size="lg" tone="ink" />
```

Sizes: `sm` (22), `md` (30), `lg` (42). Tone: `amber` (default) or `ink`.
