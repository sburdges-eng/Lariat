**StatusDot** — the atomic tone dot for status. Pair with a label or drop into a tile corner.

```jsx
<StatusDot tone="ok" />
<StatusDot tone="alert" pulse />
```

Tones: `muted`, `ok` (sage), `warn` (brass), `alert` (oxblood), `amber`.
