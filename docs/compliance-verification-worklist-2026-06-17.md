# Compliance Verification Worklist

Rows in `data/normalized/compliance_rules.jsonl` whose `verification.status` is `unverified`.
Each entry cites a source statute that has not yet been read against current law. Verify the
language at the cited URL, then flip `verification.status` to `verified` (and update
`last_verified` to today) directly in the JSONL. This document is regenerated by
`npm run compliance:worklist` — check items off below as you work.

- Generated: 2026-06-17T10:54:14.119Z
- Source: `data/normalized/compliance_rules.jsonl`
- Unverified rows: 0 of 46

## Per-domain counts

