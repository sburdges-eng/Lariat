# Phase E E1 — choose and document the one canonical home

**Why this is yours:** Naming the single canonical home for the Swift app bundle, the edge server, and `data/` (`lariat.db`, the JSONL audit dir, caches) is the owner decision every destructive Phase E step is built on; agents may draft the record but may not choose (`2026-07-02-lariat-native-phase-d-e-checklists.md:32-39`; `2026-08-05-phase-e-consolidation.md:11,15`).
**Unblocks:** E2 relocate/absorb load-bearing paths → E3 delete manifest → E4 ☠ verified backup → E5 ☠ batch deletes (`2026-08-05-phase-e-consolidation.md:16-19`).
**Where:** chat (the decision; an agent runs the checks and drafts the record)  **Time:** 30 min
**Status:** open — not recorded anywhere as of 2026-09-02: `docs/superpowers/plans/2026-08-05-phase-e-consolidation.md:15` still `1. [ ] E1`, `docs/superpowers/specs/2026-07-02-lariat-native-phase-d-e-checklists.md:38` still `- [ ] E1.`, no PR (`gh pr list --state all --search "canonical home"` → only #632/#115), `.agent-sessions/handoff.md` has no E1 or go-live entry (last entry 2026-08-29, `:2`). **De facto on 2026-09-02:** edge = this checkout's Next hub (`lsof -nP -iTCP:3000 -sTCP:LISTEN` → node PID 35362, `next-server`, cwd `~/lariat_dev/Lariat`) and data root = `~/Library/Application Support/Lariat/data` (`.env.local:13` sets `LARIAT_DATA_DIR`; the same PID holds that `lariat.db` + `-wal`/`-shm` open) — chosen for today's go-live, never written down as E1. App-bundle row still open (2026-08-31 salvage kept one native `.app`, `2026-08-31-app-lineage-salvage.md:13-19`; open PR #677 makes the Electron bundle the packaged-release gate).

## Before you start
- [ ] Read today's go-live decision first, do not re-ask it: `grep -n "Go-live topology\|DB of record" .agent-sessions/handoff.md` → the section written by `go-live-serving-topology-and-data-root.md` step 10 (2026-09-02 morning: **none yet**). Also read `go-live-venue-data.md` (which DB is truth today). E1 consumes those answers for the edge / `data/` / audit / cache rows.
- [ ] Phase D exited (plan gate: `phase-e-consolidation.md:4` "gated on D") — check: `sed -n '4p' docs/superpowers/plans/2026-08-05-phase-d-edge-reduction.md` → 2026-09-02 prints `status: planned — gated on C5`, i.e. **not met**. E1 carries no ☠ (only E4/E5 do, `phase-d-e-checklists.md:46-49`); recording it ahead of D is your call — if you do, say so in the record.
- [ ] `origin/main` is fresh and the salvage record is on it — check: `git fetch origin && git ls-tree origin/main --name-only -- docs/superpowers/plans/2026-08-31-app-lineage-salvage.md` → prints the path. Do **not** pull or reset the shared `~/lariat_dev/Lariat` checkout while the go-live session is live (2026-09-02: `## main...origin/main [behind 3]` with 5 modified files); the `sean` worktree in Close out branches from `origin/main` on its own (`scripts/worktree.sh:113-120`).
- [ ] The one kept app bundle still exists — check: `/usr/libexec/PlistBuddy -c "Print :CFBundleShortVersionString" -c "Print :CFBundleIdentifier" LariatNative/build/Lariat.app/Contents/Info.plist` → `0.2.0` then `com.lariat.native` (true 2026-09-02).
- [ ] The App Support DB is present — check: `ls -la "$HOME/Library/Application Support/Lariat/data/"` → `lariat.db` listed. Its provenance is disputed: salvage `:63-66` says "recovered Aug-5 real DB … promoted", salvage `:77-78` says the App Support tree is a "smoke fixture, not venue data", `go-live-venue-data.md:10-11` says "the hand-seeded DB from 2026-08-30". `go-live-venue-data.md` settles that today; E1 only names the *root*.
- [ ] Know whether the SSD is mounted — check: `test -d ~/Dev; echo "exit=$?"` → `exit=0` mounted, `exit=1` not (the normal state; 2026-09-02: `1`). Unmounted means every `~/Dev/...` path is last-known and unverifiable — record it as such; do not skip the decision.

## Steps
Steps 1-4: ask an agent to run them and paste the output; you skim. Step 5 is yours (**chat**). Step 6 an agent drafts, you approve.

1. **Re-read what is already decided.** `sed -n '13,19p;61,70p' docs/superpowers/plans/2026-08-31-app-lineage-salvage.md` → expect: kept app = `LariatNative/build/Lariat.app` v0.2.0; base data = recovered Aug-5 DB migrated v5→v6 and promoted into Application Support; H8 = stapled `.app` in a `.dmg`; "this Mac is the venue/testing Mac — no separate venue Mac exists". If not: the file changed — read all of it before continuing.
2. **Re-read what E1 must cover.** `sed -n '38,39p' docs/superpowers/specs/2026-07-02-lariat-native-phase-d-e-checklists.md` → expect exactly: Swift app bundle, edge server, `data/` incl. `lariat.db` + JSONL audit dir + caches. Checkouts, `Lariat-KDS`, `lariat-data-sources` are E2 (`:40-42`), not E1; KDS is out of readiness scope anyway (`CLAUDE.md:18-22`).
3. **Look at the live data split — this is the real E1 question.**
   ```bash
   ls -la data/ | grep -E "audit|cache|lariat"; ls -la data/audit/
   ls -la "$HOME/Library/Application Support/Lariat/data/"
   grep -n '^LARIAT_DATA_DIR=' .env.local; lsof -nP -iTCP:3000 -sTCP:LISTEN
   ```
   → expect (2026-09-02): repo `data/` has `audit/management-actions.jsonl` (53 KB, mtime 2026-09-01 16:40 — **still being written**), `cache/`, a symlink `lariat-data -> /Volumes/Sean's SSD/lariat-data`, and **no** `lariat.db`; Application Support `data/` has `lariat.db` (+ `-wal`/`-shm` while a process holds it), `peer-keypair.json` (`lib/peerKeypair.ts:76`), `cache/`, `keys/`, `uploads/`, and **no** `audit/`. `.env.local:13` = `LARIAT_DATA_DIR=/Users/seanburdges/Library/Application Support/Lariat/data`; a `node` listener on :3000 from this checkout holds the App Support DB. Resolution rules: web = `LARIAT_DATA_DIR` else `<cwd>/data` (`lib/dataDir.ts:32-36`); native = `LARIAT_DATA_DIR` → dev cwd-walk to `<repo>/data` → Application Support (`LariatNative/Sources/LariatModel/StationCatalog.swift:139-174`, order at `:144-173`). Audit JSONL and `cache/` follow whichever root wins (`LariatNative/Sources/LariatDB/DataDirectory.swift:14-23`, `lib/auditLog.mjs:34-41`, `StationCatalog.swift:119`) — so the audit pick implies an E2 merge of the repo `management-actions.jsonl` into the chosen root. If the listings differ: write down what you saw; the decision still needs making.
4. **Confirm the checkout facts (context only, feeds E2).** `git remote get-url origin && git log -1 --format='%h %ad %s' --date=short origin/main` → expect the GitHub remote and today's `origin/main` tip. `~/lariat_dev/Lariat` is current (`CLAUDE.md:43`); seven checkouts remain, all others read-only backups (`CLAUDE.md:56-66` Correction 2026-08-30; `STALE-DOCS-2026-09-01.md:30` #24 flags the Phase E docs still naming `~/Dev/hospitality/Lariat`). If the SSD is unmounted the `~/Dev` rows cannot be listed — record "last-known per CLAUDE.md §1". Not a decision row.
5. **Decide — chat, one line per row.** "Keep as is" / "confirm the go-live pick" are answers; "don't know" leaves that row open.
   | Row | Candidates today | Pick |
   |---|---|---|
   | Swift app bundle | `LariatNative/build/Lariat.app` (only survivor, ad-hoc signed, git-ignored build dir per `LariatNative/Scripts/PACKAGING.md:20`). Context: Electron wrapper `desktop/` source is on `main` (salvage `:34`), built `dist/` deleted, "never rebuild casually" (salvage `:75-76`) — yet open PR #677 (`feat/smoke-day`) makes `npm run verify:release` = `verify` → `desktop:dist` → `smoke:day` the packaged-release gate and gives the native `.app` only file-level assertions. Decide native `.app` vs Electron bundle as *the* app; if native, say so on #677 before it merges. H8 format: salvage `:70` says stapled `.app` in `.dmg`, but `2026-08-05-h8-notarization.md:17` and `PROJECT_STATUS.md:60` still list `.pkg` vs `.dmg` open — tick/strike them in the same PR or note the conflict | |
   | Edge server (Next.js) | this repo root (`package.json:2` `lariat-cockpit`, `:8` `next dev`); live today as the :3000 hub from `~/lariat_dev/Lariat` (go-live mode A, `go-live-serving-topology-and-data-root.md` step 4); D6 gives it its own runbook later (`phase-d-e-checklists.md:26-27`) | |
   | `data/` root + `lariat.db` | (a) `~/Library/Application Support/Lariat/data` — pinned by `.env.local:13`, open by the hub today, verified backup at `backups/2026-09-02T11-11-34-266Z/` (`SHA256SUMS`, `manifest.json`); (b) `<repo>/data` — dev default, no DB. Confirm or override the go-live pick from `handoff.md` / `go-live-venue-data.md` | |
   | JSONL audit dir | `<data root>/audit/management-actions.jsonl` — follows the `data/` pick; today only the repo copy exists and it is live (step 3) | |
   | Caches | `<data root>/cache` — both roots have one today | |
   → expect: five answers. If any row is "don't know", E1 stays open for that row — do not guess.
6. **Draft the record (an agent does this; you approve).** Append a dated `## E1 — canonical home (decided YYYY-MM-DD)` section holding the filled table to the end of `docs/superpowers/plans/2026-08-05-phase-e-consolidation.md` (after line 26), append `## YYYY-MM-DD Phase E E1 — canonical home` with the same table to `docs/PROJECT_ROADMAP.md` (append-only decision log, `:3-5`; `PROJECT_STATUS.md:117` sends readers there), and rewrite the Container Inventory table in `docs/LARIAT_NATIVE_FINAL_AGENT_GUIDE.md:19-27` to match — it still names `hospitality/Lariat/` canonical (`:21`) and repeats the 2026-07-22 archive claim (`:29-37`) that `CLAUDE.md:56-58` says is false. That guide is where `docs/PROJECT_STATUS.md:115` sends readers for "what is canonical". → expect: `git diff --stat` in the worktree lists exactly those three files plus the ticks from "Record the result".
7. **Touch nothing on disk.** No delete, move, or rsync — E1 is paper only; relocation is E2, deletion is E4/E5 ☠ (`phase-d-e-checklists.md:40-49`; `phase-e-consolidation.md:22-26`). → expect: `git status --short` in the worktree shows only `.md` changes.

## Pass / fail
Pass when one home is named and written down for each of: Swift app bundle, edge server, `data/` incl. `lariat.db`, the JSONL audit dir, caches (`phase-d-e-checklists.md:38-39`; `phase-e-consolidation.md:15` "app bundle, edge, data/, audit dir") — and nothing on disk changed. Any row still "don't know" = E1 open; do not tick the box on a partial answer. The load-bearing list (`phase-d-e-checklists.md:40-42`) is not altered by E1.

## Record the result
- Evidence: no template exists for E1 (`docs/superpowers/templates/` holds only `service-day-shutoff-log.md`). The filled table from step 6 in `docs/superpowers/plans/2026-08-05-phase-e-consolidation.md` **is** the evidence — date it, name who decided, and add one line if it was decided ahead of Phase D exit; the `PROJECT_ROADMAP.md` entry mirrors it.
- Then update:
  - `docs/superpowers/plans/2026-08-05-phase-e-consolidation.md:15` — `1. [ ] E1 …` → `1. [x] E1 … (decided YYYY-MM-DD, see § E1 below)`.
  - `docs/superpowers/specs/2026-07-02-lariat-native-phase-d-e-checklists.md:38` — `- [ ] E1.` → `- [x] E1.` plus the date.
  - `docs/LARIAT_NATIVE_FINAL_AGENT_GUIDE.md:19-37` — inventory table and consolidation paragraph, per step 6.
  - `docs/PROJECT_ROADMAP.md` — the appended `## YYYY-MM-DD Phase E E1` entry, per step 6.
  - This runbook's **Status** line → `decided YYYY-MM-DD`; its row in `docs/runbooks/person-only/README.md` (index lists only the three go-live slugs on 2026-09-02, `:10-14`; that README is owned by the go-live session today — add the row after that session closes, or ask an agent to).
  - `docs/PROJECT_STATUS.md:55` "Blocked on an owner decision" — no E1 bullet exists; nothing to strike. If the `.pkg`/`.dmg` note at `:60` changes, refresh the as-of SHA (`:10`, rule `:132`). Do not add a `shipped` row: E1 is a task, not a front exit (the sync rule at `2026-08-05-native-1-0-gap-execution-index.md:69-76` fires at Phase E exit).
  - `docs/OPERATIONS_HANDOFF.md` — no Phase E item (last updated 2026-06-12, `:3`); nothing to strike.
  - Leave `docs/superpowers/specs/2026-07-02-lariat-native-endgame.md:120` unchecked — that box is all of Phase E.
  - Do not edit `CLAUDE.md` or `AGENTS.md` in this PR: another session owns them (`git status` 2026-09-02: ` M CLAUDE.md`, ` M AGENTS.md`, both already identical to `origin/main` after #674; PR #677 edits `CLAUDE.md` again — re-check the `CLAUDE.md:NN` cites here after it merges).

## Close out
```bash
scripts/worktree.sh new sean chore/phase-e-e1-canonical-home
```
Commit only the doc files named above plus this runbook on that branch; never `git add -A` in `~/lariat_dev/Lariat` (dirty with another session's `.claude/settings.json`, `.gitignore`, `AGENTS.md`, `CLAUDE.md`, `package.json`). Run the gate in the worktree, not the main checkout: `scripts/verify.sh` is on `origin/main` since #674 (`git ls-tree origin/main -- scripts/verify.sh`), so `/ship` (`bash scripts/verify.sh`, `.claude/skills/ship/SKILL.md:6-7`; it stamps `version.json` into whatever tree it runs in) works unchanged in the fresh worktree; `npm run verify` (`package.json:39`) is the fallback. Paste the exit code. Never push to main.

## If something goes wrong
- E1 changes no disk state; abort = close the PR unmerged. No service-day rollback applies.
- SSD unmounts mid-run (`test -d ~/Dev` → `exit=1`): finish the decision; mark `~/Dev` rows "last-known" (`CLAUDE.md:36-37`). A home that vanishes when the drive is out (`CLAUDE.md:28-30`) is a fact to weigh, not a blocker.
- A doc and the disk disagree (a path the salvage record says was deleted is present, or vice versa): the repo wins — stop, note it verbatim in the record, fix the doc in the same PR.
- The go-live session picks a different DB of record or data root today (`go-live-venue-data.md` option 2 copies a DB to `<repo>/data/lariat.db`): E1's `data/` row must match whatever `.agent-sessions/handoff.md` § Go-live topology finally says — re-run step 3 before deciding.
- Any step wants to edit `CLAUDE.md` or delete/move a path: stop; leave the note for the next agent in `.agent-sessions/handoff.md` (`AGENTS.md:85`, uncommitted on this checkout as of 2026-09-02; `origin/main` has the same file at `:93` "Cross-tool handoff file") instead.
