# Resolve the dirty shared main checkout

**Why this is yours:** Two sessions' uncommitted files sit in the one checkout every session shares (`AGENTS.md:30-34`; `.agent-sessions/handoff.md:16`). Deciding what ships where is the owner's call, and agents may not commit unasked (`CLAUDE.md:165`) or move `main`'s HEAD under another session.
**Unblocks:** a clean `main` that can fast-forward again; PR #674 and the go-live docs each land carrying only their own files; the `.agent-sessions` claims on `AGENTS.md`/`.gitignore` can drop.
**Where:** this laptop — main checkout `/Users/seanburdges/lariat_dev/Lariat`.  **Time:** ~15 min for group A now; ~15 min for group B once the go-live session is done writing, plus two CI runs.
**Status:** open — per `git status` 2026-09-02 05:19 MDT: `M .claude/settings.json .gitignore AGENTS.md CLAUDE.md package.json`, `?? .claude/skills/person-only/SKILL.md docs/boh/print/outage-card.html docs/runbooks/person-only/ scripts/bootstrap.sh scripts/verify.sh`; and `docs/runbooks/person-only/STALE-DOCS-2026-09-01.md:74` (item 68). PR #674 is OPEN, `mergeStateStatus: CLEAN`, both checks SUCCESS (`gh pr view 674`, 2026-09-02). The 2026-09-01 catalog list is stale: `.gitignore` (+3 lines) and `.claude/skills/person-only/SKILL.md` appeared 05:17–05:19 today and the go-live session is still writing.

## Before you start
- [ ] gh signed in — check: `gh auth status` → `Logged in to github.com account sburdges-eng` (today: yes).
- [ ] In the main checkout, on `main` — check: `git rev-parse --show-toplevel && git branch --show-current` → `/Users/seanburdges/lariat_dev/Lariat` then `main`.
- [ ] Local `main` = `origin/main` — check: `git rev-list --left-right --count main...origin/main` → `0	0` (today: both `fdc708f2`). If not, `git fetch origin` and read what moved before touching anything (`CLAUDE.md:153-155`).
- [ ] Nothing stashed you care about — check: `git stash list` → empty (today: empty). Stash is a trap in this checkout (`CLAUDE.md:158-162`).
- [ ] Know who claims what — check: `node scripts/agent-session.mjs list` → `claude` active, claims `AGENTS.md`, `.gitignore`, `.claude/skills/person-only/SKILL.md`, `README.md`, `STALE-DOCS-2026-09-01.md`.
- [ ] Ask the go-live session (chat) whether it is done writing `docs/runbooks/person-only/`, `docs/boh/print/outage-card.html`, `AGENTS.md`, `.gitignore`, `.claude/skills/person-only/SKILL.md`. Group B (steps 5–10) waits on that answer; group A (steps 1–4) does not.

## Steps
1. **Snapshot the dirty tree before anything else.** `mkdir -p ~/lariat_dev/dirty-main-2026-09-02 && git diff > ~/lariat_dev/dirty-main-2026-09-02/tracked.patch && git ls-files --others --exclude-standard | tar -czf ~/lariat_dev/dirty-main-2026-09-02/untracked.tgz -T - && git status --porcelain=v1 --untracked-files=all > ~/lariat_dev/dirty-main-2026-09-02/status.txt` → expect: `tar -tzf ~/lariat_dev/dirty-main-2026-09-02/untracked.tgz | wc -l` ≈ 20 and `status.txt` lists every dirty path. If not: stop — nothing has changed yet.
2. **Sort every dirty file into an owner.** Group A = PR #674 (`chore/insights-tooling`): `.claude/settings.json`, `CLAUDE.md`, `package.json`, `scripts/bootstrap.sh`, `scripts/verify.sh`. Prove they are already on that branch: `for f in .claude/settings.json CLAUDE.md package.json scripts/bootstrap.sh scripts/verify.sh; do git show origin/chore/insights-tooling:$f | cmp -s - $f && echo "same  $f" || echo "DIFF  $f"; done` → expect: five `same` (true 2026-09-02; local and origin branch both `43035e96`). Group B = the go-live/person-only session, in no branch or PR: `AGENTS.md` (+8 lines `## Person-only operations (owner gate)`), `.gitignore` (+3 lines un-ignoring `.claude/skills/person-only/`), `.claude/skills/person-only/SKILL.md`, `docs/boh/print/outage-card.html` (named by `docs/runbooks/person-only/go-live-print-pack.md:16`), `docs/runbooks/person-only/` (this file included). Prove it: `git log --all --oneline -- docs/runbooks/person-only docs/boh/print/outage-card.html .claude/skills/person-only | head -3` → empty, and `for b in $(git branch --format='%(refname:short)'); do git show $b:AGENTS.md 2>/dev/null | grep -q 'Person-only operations' && echo "on $b"; done` → prints nothing. If a `DIFF` shows or a path is in neither list: read it (`git diff <file>`, or open it) and place it yourself — that is the decision only you can make. Group A drift belongs in the `claude-insights-tooling` worktree, not here.
3. **Land #674 (your call).** `gh pr view 674 --json state,mergeStateStatus,statusCheckRollup --jq '{state,mergeStateStatus,checks:[.statusCheckRollup[]|.conclusion]}'` → `OPEN`, `CLEAN`, `SUCCESS`. Then `gh pr merge 674 --squash` → `Squashed and merged pull request #674`. No `--delete-branch`: the branch is checked out at `../Lariat-worktrees/claude-insights-tooling` and gh would also try to pull your dirty `main`. Squash matches how #650–#673 landed (single-parent commits titled `(#N)`). #677 also edits `CLAUDE.md` and `package.json` — it rebases later, in its own lane. If not CLEAN: stop; that is PR-review work, not this runbook. If you are not merging today: skip to step 5 — group A costs nothing until you next fast-forward `main`.
4. **Sync group A out of main.** `git fetch origin && for f in .claude/settings.json CLAUDE.md package.json scripts/bootstrap.sh scripts/verify.sh; do git show origin/main:$f | cmp -s - $f && echo "same  $f" || echo "DIFF  $f"; done` → five `same` (the merge carried exactly your copies). Then `git checkout HEAD -- .claude/settings.json CLAUDE.md package.json && rm scripts/bootstrap.sh scripts/verify.sh && git merge --ff-only origin/main` → `Fast-forward`; `git status --porcelain` now shows only group-B lines. If a `DIFF`: something else touched that file on `main` — `git show origin/main:$f | diff - $f`, keep your side only if it is real, then proceed. If the merge refuses: it names the file in the way; restore or move that one file, never `reset --hard` or `stash` (`CLAUDE.md:158-162`).
5. **Wait for the go-live session, then freeze group B.** Ask it in chat: done writing, and will it open its own PR? If it will: let it, and skip to step 9 when that PR merges. Confirm nothing is still moving: `ls -lt docs/runbooks/person-only docs/boh/print/outage-card.html .claude/skills/person-only | head -5` twice, five minutes apart → same mtimes. Note the claim clock: the pre-commit guard refuses commits touching files an agent claimed within the last 30 min (`scripts/check-session-branch.mjs:82-126`); `jq -r .lastUpdate .agent-sessions/claude.json` tells you when that window closes.
6. **Take a `sean` worktree for group B.** `scripts/worktree.sh new sean chore/resolve-dirty-shared-main-checkout` → `✓ worktree ready`, `path: /Users/seanburdges/lariat_dev/Lariat-worktrees/sean-resolve-dirty-shared-main-checkout`, `branch: chore/resolve-dirty-shared-main-checkout (locked via SESSION_BRANCH)` (`scripts/worktree.sh:146-154`). It fetches first, so the branch starts at current `origin/main` (`:113-120`). If `already exists` (`:104`): `scripts/worktree.sh list` and reuse it.
7. **Copy group B into the worktree — only group B.** Same terminal: `WT=../Lariat-worktrees/sean-resolve-dirty-shared-main-checkout && git diff -- AGENTS.md .gitignore | git -C "$WT" apply && mkdir -p "$WT/docs/runbooks" "$WT/docs/boh/print" "$WT/.claude/skills" && cp -R docs/runbooks/person-only "$WT/docs/runbooks/" && cp docs/boh/print/outage-card.html "$WT/docs/boh/print/" && cp -R .claude/skills/person-only "$WT/.claude/skills/" && git -C "$WT" status --porcelain=v1 --untracked-files=all` → exactly `M .gitignore`, `M AGENTS.md`, `?? .claude/skills/person-only/SKILL.md`, `?? docs/boh/print/outage-card.html`, `?? docs/runbooks/person-only/…`. If `git apply` fails: `git diff -- AGENTS.md .gitignore | git -C "$WT" apply --3way`; the hunks are 8 + 3 lines, hand-paste if needed. Before committing, set this file's **Status** line to `done 2026-09-02 — steps 9–10 run after this PR merges` in `$WT/docs/runbooks/person-only/resolve-dirty-shared-main-checkout.md`.
8. **Commit and open the PR (this is what `/ship` does — `.claude/skills/ship/SKILL.md:1-10`, laptop-local, gitignored at `.gitignore:43`).** `git -C "$WT" add AGENTS.md .gitignore .claude/skills/person-only docs/runbooks/person-only docs/boh/print/outage-card.html && git -C "$WT" diff --cached --stat` → only group-B paths (`CLAUDE.md:163`). `git -C "$WT" commit -m "docs(runbooks): person-only runbook set, outage card, owner-gate section in AGENTS.md"` → the pre-commit hook runs the branch guard, lint-staged and `npm run typecheck` (`.git/hooks/pre-commit:12`; 1–2 min). Then `git -C "$WT" fetch origin && git -C "$WT" rebase origin/main && bash "$WT/scripts/verify.sh"; echo "exit=$?"` → `VERIFY_EXIT=0` and `exit=0` (several minutes; `scripts/verify.sh` is in the worktree only once #674 is on `origin/main` — otherwise `(cd "$WT" && npm run version:stamp && npm run verify > /tmp/verify.log 2>&1; echo "exit=$?")`, `CLAUDE.md:184,202`). Then `(cd "$WT" && gh pr create --fill)` → a PR URL. If the hook says `file claim conflict`: `AGENT_NAME=claude git -C "$WT" commit …` (`.claude/skills/person-only/SKILL.md:116`) or wait out the 30 min. If it says `SESSION_BRANCH expected …`: `git -C "$WT" switch chore/resolve-dirty-shared-main-checkout`.
9. **After the group-B PR merges, sync group B out of main.** `git fetch origin && for f in AGENTS.md .gitignore $(git ls-files --others --exclude-standard); do git show origin/main:"$f" 2>/dev/null | cmp -s - "$f" && echo "same  $f" || echo "DIFF  $f"; done` → every line `same`. Then `git checkout HEAD -- AGENTS.md .gitignore && rm -r docs/runbooks/person-only docs/boh/print/outage-card.html .claude/skills/person-only && git merge --ff-only origin/main` → `Fast-forward`; the merge recreates all of them from `origin/main`. If any `DIFF`: the go-live session wrote again after step 7 — copy that file's current version into the worktree, push a follow-up commit to the same PR, repeat this step after it merges.
10. **Prove main is clean.** `git status --porcelain; git branch --show-current; git rev-parse --short HEAD origin/main` → empty, `main`, two identical SHAs. `gh pr view 674 --json files -q '.files[].path'` → only the five group-A paths; `gh pr view <B> --json files -q '.files[].path'` → only group-B paths. Tell the go-live session (chat) `main` is clean so it retires its claims (`node scripts/agent-session.mjs update …` is its job, not yours).

## Pass / fail
The sources' own rules: one worktree per session, the main checkout never a commit target for a batch (`AGENTS.md:30-39`; `CLAUDE.md:149-152`); `git status` + branch before every commit and inspect what is staged (`CLAUDE.md:153,163`); never push `main`, never `reset --hard` or stash here (`CLAUDE.md:147,158-162`). **PASS:** step 10 prints an empty `git status --porcelain` on `main` at `origin/main`; every file from step 1's `status.txt` reached `origin/main` through a PR carrying only its owner's files; every `cmp` line in steps 2, 4 and 9 read `same`. **FAIL:** a `DIFF` you cannot account for, a PR mixing the two groups, or a merge refusal you got past with `reset --hard`/`stash`/`--force`.

## Record the result
- Evidence: no template exists (catalog `evidence_target` is empty). Put in the group-B PR body (`gh pr edit <B> --body-file <file>`): the snapshot dir `~/lariat_dev/dirty-main-2026-09-02/` and its `status.txt`, the `same` lines from steps 2, 4 and 9, and step 10's three lines verbatim. Append a dated line to `.agent-sessions/handoff.md` (gitignored, `.agent-sessions/.gitignore:1`): "main checkout clean at <SHA>; #674 + #<B> merged; drop claims on AGENTS.md/.gitignore/SKILL.md".
- Then update: this file's **Status** line (set in step 7; correct it if steps 9–10 changed anything); `docs/runbooks/person-only/README.md` — add or flip this slug's row in the `| Slug | Step | Status |` table (the go-live session may hold that file open — coordinate in chat rather than racing it); `docs/runbooks/person-only/STALE-DOCS-2026-09-01.md:74` item 68 — append `— resolved 2026-09-02: #674 merged, group B via #<B>`.

## Close out
```bash
scripts/worktree.sh new sean chore/resolve-dirty-shared-main-checkout
```
This is the worktree from step 6 — if it already exists, `scripts/worktree.sh list` and reuse it; never create a second one for the same branch. Commit the evidence and doc updates on that branch and open a PR (the repo's /ship skill runs the verify gate). Never push to main.

## If something goes wrong
- Steps 1–2 change nothing; stopping there leaves `main` dirty but working, as it is today.
- Never `git reset --hard`, never `git stash`, never `git checkout <branch>` in the main checkout (`CLAUDE.md:158-162`; `AGENTS.md:32`) — they nuke or misfile another session's files.
- Restore anything lost from the snapshot, from the repo root: `tar -xzf ~/lariat_dev/dirty-main-2026-09-02/untracked.tgz` and `git apply ~/lariat_dev/dirty-main-2026-09-02/tracked.patch`.
- `git merge --ff-only` refuses → it names the file; `cmp` it against `origin/main`, restore (tracked) or `rm` (untracked) that one file, retry. Do not force.
- `git apply` fails in the worktree → `--3way`, or hand-paste from `git diff AGENTS.md .gitignore`.
- Pre-commit refuses on a file claim → `AGENT_NAME=claude git -C "$WT" commit …`, or wait 30 min after `jq -r .lastUpdate .agent-sessions/claude.json`.
- Verify gate red on a docs-only branch → read the log tail it prints; if it is a `main` regression, stop and hand it to an agent — do not "fix" it in this PR.
- The go-live session is still writing → stop after step 4; group A alone is a fine stopping point. Tell that session in chat before step 6 and after step 10.
