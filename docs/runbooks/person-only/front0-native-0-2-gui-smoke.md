# Front 0 Part A — Native 0.2 GUI smoke (no python3)

**Why this is yours:** The smoke needs a macOS GUI login, a Finder launch with a clean environment, and Activity Monitor open while you drive the Assistant and Parties boards. The runner table assigns it to **Owner** (`2026-08-05-g0-gui-smoke-and-shutoff.md:22`); agents in the Linux cloud cannot fill the boxes (`2026-08-05-native-1-0-gap-execution-index.md:82-84`). A partial run happened 2026-08-31 (`2026-08-31-ka-v4-usefulness.md:11-19`; the Application Support DB holds `scaled:herbal_butter` rows and a `scale_recipe: 5 leaf rows` audit at 09:34Z — that DB is a smoke fixture per `2026-08-31-app-lineage-salvage.md:77-78`; `go-live-venue-data.md` decides whether it is the DB of record) but the A3 table is still blank and no cascade / python3 observation was recorded.
**Unblocks:** Native 0.2 freeze claim (g0 plan `:85`); the agent A4 doc flip (`:79-83`); G0 Part B service-day shutoff.
**Where:** venue Mac (macOS GUI login) — the 0.2.0/2047 build and the Application Support tree are on this laptop.  **Time:** 20 min (+ ~5 min rebuild).
**Status:** open — 2026-09-02: origin/main g0 plan `:4` still `ready-for-owner`, A3 table `:67-75` blank; `2026-07-07-native-0.2-l1-status.md:118` "Owner-pending — Front 0 Part A"; `2026-07-10-phase4-verify-0.2-evidence.md:26` "PENDING owner"; live DB newest audit row is still `2026-08-31 09:34:48|scale_recipe: 5 leaf rows` (no cascade / python3 evidence since). **Do not run today during service:** `next-server (v16.2.6)` PID 35362 (started 2026-09-02 05:20) is listening on :3000 and holds `~/Library/Application Support/Lariat/data/lariat.db` open — launching Lariat.app would be a second writer on the venue WAL (`go-live-serving-topology-and-data-root.md` step 4: one at a time on launch day).

## Before you start
Ask an agent to run these and paste the output; you only need steps 2-11 in the GUI. All passed 2026-09-02 except the last one.
- [ ] Bundle version — check: `/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' -c 'Print :CFBundleVersion' LariatNative/build/Lariat.app/Contents/Info.plist` → today `0.2.0` / `2047` (see step 1: rebuild is the default)
- [ ] Signature verifies (g0 `:51`) — check: `codesign --verify --strict LariatNative/build/Lariat.app && echo OK`
- [ ] Schema resource is inside the bundle (g0 `:52`) — check: `find LariatNative/build/Lariat.app/Contents/Resources -name frozen_schema.sql` → one path under `LariatNative_LariatDB.bundle`
- [ ] Recipe tree + BEO map in Application Support (g0 `:53-54`) — check: `ls "$HOME/Library/Application Support/Lariat/recipes/recipe_index.csv" "$HOME/Library/Application Support/Lariat/menus/beo_recipe_map.csv"`
- [ ] No data dir baked into the bundle (Finder launch must resolve Application Support on its own) — check: `plutil -extract LSEnvironment raw -o - LariatNative/build/Lariat.app/Contents/Info.plist` → error "No value at that key path" is the GOOD result
- [ ] launchd env is clean of `LARIAT_*` (g0 `:34`) — check: `for v in LARIAT_ROOT LARIAT_DATA_DIR LARIAT_PYTHON; do echo "$v=$(launchctl getenv $v)"; done` → all three empty (`.env.local` sets `LARIAT_DATA_DIR` for the web hub only; a Finder launch does not read it)
- [ ] Ollama is up with the LaRi model (the model emits the `scale_recipe` action; without it nothing scales) — check: `curl -s -m 2 http://127.0.0.1:11434/api/tags | grep -o 'lari-the-kitchen-assistant[^"]*'` → `lari-the-kitchen-assistant:latest`
- [ ] A BEO event with line items exists — check: `sqlite3 -readonly "$HOME/Library/Application Support/Lariat/data/lariat.db" "SELECT e.id, e.title, e.event_date, count(li.id) FROM beo_events e JOIN beo_line_items li ON li.event_id=e.id GROUP BY e.id;"` → today: `9|Shelia Menz Welcome Party|2026-07-03|7` and `7|GOODE|2026-05-16|2` (ids change if `go-live-venue-data.md` installs another DB — use whatever this returns)
- [ ] A manager PIN exists (scaling is PIN-gated, see step 6) — check: `sqlite3 -readonly "$HOME/Library/Application Support/Lariat/data/lariat.db" "SELECT count(*) FROM manager_pin_users;"` → `2` today; you know one of the PINs (`go-live-pin-login.md` verified one 2026-09-02)
- [ ] No Lariat running (g0 `:58`) — check: `pgrep -x LariatApp || echo none`
- [ ] **Nothing else is serving the same DB** — check: `lsof -nP -iTCP:3000 -sTCP:LISTEN; lsof "$HOME/Library/Application Support/Lariat/data/lariat.db"` → both empty. FAILS today (PID 35362). Run the smoke outside a service window with the web hub stopped by you — agents never stop it (`go-live-serving-topology-and-data-root.md`). `package-app.sh:34-37` says WAL sharing survived a 3-writer test, `install-prod-data.sh:11-14` says never; on launch day the topology runbook wins.

## Steps
All steps run on the venue Mac unless noted.

1. **Rebuild at HEAD (default).** Build 2047 = commit `065294d0` (#659) and predates five native fixes on exactly the surfaces smoked: #662 (garble guard ate truthful answers — a correct `Scaled …` reply can be swallowed → false fail), #664, #668 (cascade strip showed another party's counts), #670, #673 (`⚡ ACTION EXECUTED:` → `⚡ ACTION:`). Run `cd LariatNative && Scripts/package-app.sh --version 0.2.0` → expect `package-app: built …/build/Lariat.app (…)` then `package-app: done.`; build number = `git rev-list --count HEAD` of the checkout (main checkout is `fdc708f2` = 2062 today, 3 docs/tooling commits behind origin/main, all five fixes included). Do **not** pass `--data-dir` — that bakes a dev path into the bundle. Exits non-zero on any failed stage (`set -euo pipefail` `:44`; `die` at `:82,:90-92,:96,:104,:166,:190,:203`) — read the last `package-app:` line; a red `swift build` is a code defect per CLAUDE.md §2, agent work. Fallback only if the rebuild fails: smoke 2047 and note the caveat above in A3 Notes.
2. **Quit any running Lariat.** ⌘Q the app, then `pgrep -x LariatApp || echo none` → `none`. If not: `pkill -x LariatApp` and re-check.
3. **Open Activity Monitor and take the python baseline.** Type `python` in its search box → note what is listed (should be nothing owned by Lariat). Terminal equivalent: `pgrep -fl python3 || echo 'no python3'`.
4. **Launch from Finder.** Double-click `LariatNative/build/Lariat.app`, or `open LariatNative/build/Lariat.app` (LaunchServices launch = same clean env) → expect: window appears, no crash (g0 `:59-60`). If not: `ls -t ~/Library/Logs/DiagnosticReports | head -3` for the crash report; record → `smoke fail`.
5. **Go to Assistant.** Sidebar → **Assistant** → expect the header dot to read **Ollama online**. If it reads **Ollama offline**: start Ollama, click the dot to re-check.
6. **Unlock manager tier.** Click the **Cook tier** lock badge in the Assistant header (`KitchenAssistantView.swift:82-91` → PIN sheet), enter a manager PIN → badge reads **Manager tier**. Required: an un-PIN'd `scale …` is blocked before Ollama (`AssistantMessageClassifier.swift:24-27` `pinRequiredLead` includes `scale`; `KitchenAssistantEngine.swift:90-101` answers `Action blocked — manager PIN required`). The 08-31 rows landed only because a PIN session was active.
7. **Run a scale.** Type `scale birria by 2` and send → expect a reply beginning `⚡ ACTION: Scaled birria to … (×2)` and ending `values from deterministic calculator.` (`AssistantActionRepository.swift:395`; 2047 shows `⚡ ACTION EXECUTED:` instead). If the reply is `Action blocked — manager PIN required. Ask a manager to confirm.` you are not unlocked — redo step 6 and resend; not a smoke fail. If the reply says `unknown recipe slug` the model improvised a slug (the 08-31 failure): retype using an exact `recipe_id` from column 1 of `recipe_index.csv` (`birria`, `cornbread`, `green_chilli`, `herbal_butter` all exist). If the reply says `Scale Recipe failed (calculator_unavailable)` → `smoke fail`.
8. **Check python3 during/after the scale (g0 `:63`).** Activity Monitor search `python` → no `python3` whose parent is Lariat. Terminal: `pgrep -fl python3 || echo 'no python3'`; if one shows, `ps -o pid,ppid,comm -p <pid>` and compare PPID with `pgrep -x LariatApp`.
9. **Prove the leaf rows landed.** `sqlite3 -readonly "$HOME/Library/Application Support/Lariat/data/lariat.db" "SELECT created_at, station_id, item, need FROM line_check_entries WHERE station_id LIKE 'scaled:%' ORDER BY created_at DESC LIMIT 10;"` → rows stamped today with `scaled:birria`. Then `sqlite3 -readonly "$HOME/Library/Application Support/Lariat/data/lariat.db" "SELECT created_at, note FROM audit_events WHERE note LIKE 'scale_recipe%' ORDER BY created_at DESC LIMIT 3;"` → `scale_recipe: N leaf rows` stamped today. Copy both outputs into the A3 Notes.
10. **Go to Parties and run the cascade.** Sidebar → **Parties** → select the event the prereq query returned (today: **Shelia Menz Welcome Party**, event 9, 7 items). The Sheet tab runs the cascade on selection → expect the strip to show **N to buy** and **M prep tasks** chips (`BeoBoardView.swift:304-307`); click each to land on the Order guide / Prep tabs (g0 `:62`). If the strip shows a **Build the buy + prep lists** button (`:321`), press it. If it shows a red-triangle engine error → copy the text, `smoke fail`.
11. **Check python3 again (g0 `:63`).** Same as step 8 → none owned by Lariat.
12. **Quit Lariat.** ⌘Q, `pgrep -x LariatApp || echo none`.
13. **Capture machine + build for the table.** `scutil --get ComputerName; sw_vers -productVersion; /usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' LariatNative/build/Lariat.app/Contents/Info.plist; git rev-parse --short HEAD` (run the last in the checkout you built from, at build time). If you kept 2047 write `0.2.0 (2047) = 065294d0, built 2026-08-31 18:50`.
14. **Reply in chat / handoff:** `smoke pass` or `smoke fail <note>` (g0 `:77`).

## Pass / fail
Gate A (g0 `:164`): A3 table filled; **no `python3`** owned by Lariat during steps 7-11; evidence-pack boxes checked. Row by row (g0 `:72-74`): `scale_recipe` = pass only if a correctly-phrased, PIN-unlocked scale returned leaf rows (reply + step 9 rows); `BEO cascade` = pass only if order guide + prep demands rendered with no engine error; `No python3` = pass only if nothing python3 appeared under Lariat in steps 8 and 11. Any row fail → `smoke fail <note>`. LaRi answer-quality misses (wrong slug, denials — `ka-v4-usefulness.md:11-19`) and the PIN block are KA/setup issues, not a smoke fail, provided the exact-slug retry scales.

## Record the result
- First, ask an agent to run `scripts/worktree.sh new sean chore/front0-native-0-2-gui-smoke` (creates `../Lariat-worktrees/sean-front0-native-0-2-gui-smoke` from origin/main, `worktree.sh:100-120`). **Every edit below happens in that path**, never in the main checkout on `main`.
- Yours: fill the A3 table at `docs/superpowers/plans/2026-08-05-g0-gui-smoke-and-shutoff.md:67-75` — Date, Machine (step 13), Commit / `.app` version, `scale_recipe` pass/fail, `BEO cascade` pass/fail, `No python3` pass/fail, Notes (paste step 9 output, which event cascaded, and that the run added `scaled:<slug>` line_check_entries + one audit_events row to the DB of record). Reply `smoke pass|fail` (step 14).
- Then ask an agent to do the A4 flip in the same PR, only on `smoke pass` (g0 `:79-83`; same-PR rule `2026-08-05-native-1-0-gap-execution-index.md:69-76`):
  - g0 plan: tick prerequisite boxes `:33-34`, A1 boxes `:51-54`, A2 boxes `:58-63`; flip the status line `:4` and the "Cannot execute A2/B2" box `:156` (Part A done, Part B open).
  - `docs/superpowers/plans/2026-07-10-phase4-verify-0.2-evidence.md:4` status → GUI smoke PASS; `:26` gate row → **PASS** with date; check boxes `:46-50`.
  - `docs/superpowers/plans/2026-07-07-native-0.2-l1-status.md:118` → **PASS YYYY-MM-DD** (link the g0 plan).
  - `docs/NATIVE_RELEASES_AND_TAXONOMY.md:21` Native 0.2.x status → verified (GUI smoke done).
  - `docs/PROJECT_STATUS.md:57` bullet "Native 0.2 GUI smoke test" → strike/remove; `:48` row "Native 1.0 gap fronts" → note Part A done, Part B (service day) still `blocked-owner`.
  - `docs/superpowers/plans/2026-08-05-native-1-0-gap-execution-index.md:23` Front 0 row → Part A done.
  - `docs/LARIAT_NATIVE_FINAL_AGENT_GUIDE.md:65,:70,:78` Front 0 lines → only G0 shutoff remains.
  - `docs/superpowers/specs/2026-07-02-lariat-native-endgame.md:118` H8 note → GUI smoke done.
  - This file's **Status** line → `done YYYY-MM-DD (smoke pass, PR #…)`; add/flip the row in `docs/runbooks/person-only/README.md` (no front0 row exists yet) **after today's go-live edits land** — README is owned by the go-live session on 2026-09-02.
  - `docs/OPERATIONS_HANDOFF.md` has no Front 0 row (sections at `:9,25,45,53,66,72`) — nothing to strike there.

## Close out
In the `sean` worktree: `git add` only the evidence and doc files, commit. Then ask an agent to run `/ship` from the main-checkout session (`.claude/skills/` is untracked, so it is absent in a fresh worktree) or `npm run version:stamp && npm run verify:gate` in the worktree, then `gh pr create`. Never push to main.

## If something goes wrong
- App fails to launch or crashes: `ls -t ~/Library/Logs/DiagnosticReports | head -3`, attach the report path in Notes, reply `smoke fail <note>`. No rollback needed — a crash writes nothing.
- `Action blocked — manager PIN required`: not unlocked — step 6, resend. Not a smoke fail.
- `python3` appears under Lariat: record `ps -o pid,ppid,comm -p <pid>` in Notes → **No python3 = fail** → `smoke fail`; that means a spawn path survived L1 and is agent work.
- `Scale Recipe failed (calculator_unavailable)`: the in-process calculator is not wired in this build (`AssistantActionRepository.swift:360`) → `smoke fail`, agent work.
- Cascade engine error or empty buy/prep: confirm `"$HOME/Library/Application Support/Lariat/recipes/recipe_index.csv"` exists (the packaged root resolves there only when it does) and that the event has line items (prereq query); otherwise `smoke fail <message>`.
- Ollama offline / model missing: start Ollama (`ollama serve`) and confirm `lari-the-kitchen-assistant:latest` in `/api/tags`; this is a setup gap, not a smoke fail.
- Data written by the run: `line_check_entries` rows with `station_id = 'scaled:<slug>'` (`AssistantActionRepository.swift:374`, shift_date = run day, status `na`) plus one append-only `audit_events` row (`:390`) land in the DB of record — harmless to boards (no board reads `scaled:` stations) but permanent; leave them, they are the evidence, and note them in A3 Notes.
- Tell: reply `smoke fail <note>` in chat (g0 `:77`); agents do the doc flip only on `smoke pass`.
