---
title: "Phase C1 — web API route rule ledger (schema-inversion prep)"
date: 2026-07-03
status: **verify pass COMPLETE 2026-07-07** — 130/130 classified; **71/71 ported-write claims adversarially verified** (the final 41 verified on feat/lariat-native-c1-verify-41; the earlier 3 refuted rows resolved). Refutations found in the 41 were fixed in that wave (see the "Verify-41 wave" section below); all 71 V cells are now ✓.
parent: docs/superpowers/specs/2026-07-02-lariat-native-phase-c-schema-inversion.md
generator: workflow phase-c1-rule-ledger (13 classify agents + adversarial verify + critic)
---

# Phase C1 — Rule inventory (the map C5 cutover depends on)

Every `app/api/**` route file classified for the Phase C schema-ownership
inversion: which write rules already live in a native repository (**ported**),
which stay on the Next.js edge (**edge-retained**), which are **dead**, and
which are pure reads (**read-only**). Phase C5 deletes a web write route only
after its row here is `ported` **and** its native owner covers every rule.

## Summary

| Metric | Count |
|---|---|
| Total route files | 130 |
| ported | 71 |
| edge-retained | 21 |
| read-only | 37 |
| dead | 1 |
| — of which write | 86 |
| Ported **write** routes (C5 delete candidates) | 71 |
| — adversarially verified (V = ✓) | **71 (all, 2026-07-07)** |
| — verify **refuted** (all fixed or resolved) | 0 |
| — verify not run | 0 (was 41; completed 2026-07-07) |
| Low-confidence (human review) | 9 |

> **Verification coverage is COMPLETE (2026-07-07).** The adversarial-verify
> pass (an independent agent trying to *refute* each ported-write claim) now
> covers all 71 ported-write rows. The final 41 were verified on
> `feat/lariat-native-c1-verify-41` (7 batch refute-agents + a VM-layer gate
> completeness sweep); every refutation found was fixed under TDD in the same
> wave. All V cells below are `✓`. **Caveat for C5:** verification confirms the
> native repository *write* rules are faithful — but the sweep also found
> read-side gate holes at the SwiftUI ViewModel layer (which the repository
> parity tests never reach). Those are fixed too; see **Verify-41 wave** below.

## Verify-41 wave (2026-07-07) — the final 41 verified + fixed

Branch `feat/lariat-native-c1-verify-41` (SPEC
`docs/superpowers/specs/2026-07-07-lariat-native-c1-verify-41-design.md`, PLAN
`docs/plans/2026-07-07-010-feat-lariat-native-c1-verify-41-fixes-plan.md`).
Method: 7 parallel read-only refute-agents (batches B1–B7) + two VM-layer gate
completeness sweeps, then 13 TDD fix commits (T1–T13).

**Headline:** the native *repository* layer held up across all 41 — every
write-rule (validation, in-tx audit, HACCP/comps/cash math, IDOR guards) is
faithful. **All confirmed holes were one layer up, in the SwiftUI View/ViewModel,
which the repository parity tests never exercise.** New shared, tested seam:
`RegulatedReadGate` (LariatModel, `RegulatedReadGateTests`) — the read-side analog
of `ManagementWrite`, now called by every manager-tier read board.

### Refutations found and fixed

| Fix | Route(s) | Severity | What was wrong → fix |
|---|---|---|---|
| T2 | sick-worker | **PHI** | active GET `SELECT *` rendered symptoms/diagnosis to non-PIN viewers → thin projection (blank PHI cols) + `metaLine` behind `pinOk` |
| T3 | performance-reviews | **HR** | HR reviews readable with no PIN (ungated poll) → `RegulatedReadGate` poll-guard |
| T4 | host-waitlist | **guest PII** | party names/phones/notes auto-polled ungated *(row was outside the 41 — a `✓` write-verified row; sweep caught the read leak)* → poll-guard |
| T1 | costing/ingredient-masters | auth | `markReviewed` wrote an audited correction with `pinUser=nil` (web 401s) → `requireSession` + read poll-guard |
| T8 | temp-log | parity | probe `calibration_warning` advisory + audit note was dead code (`= nil`) → port `calibrationWarningFor` (failed/overdue/unknown), stamp note, return advisory |
| T9 | specials/…/promote | cosmetic | `menu_item_name` 400 copy surfaced raw "name required" → promote-local relabel (shared validator untouched) |

### Coverage gaps filled (parity code was correct, now tested)
- T10 `CleaningComputeTests` (13) — every `validateCleaningLog` 400 branch.
- T11 `CalibrationComputeTests` (9) — §4-502.11 ±2.0°F boundary, ice-point 32°F, altitude boiling point, off-charts rejects.
- T12 `PrepRepositoryTests` (2) — prep-tasks/[id] audit `{before,after}` / bare-row `payload_json` shape.

### Benign read leaks closed (user opted into max scope)
Costing/menu-tier reads the web PIN-gates but native left open (self-documented
"costing-tier reads aren't gated today" posture, no PHI): **T5** pack-changes,
**T6** specials-saved, **T7** costing/depletion-exceptions + variance-attribution
— all now `RegulatedReadGate`-gated with in-place PIN unlock.

### Defer / ratify — NOT fixed here, need a decision before C5

- **`sync_feed` on native regulated writes → the 4 HACCP routes are C5-DELETE-BLOCKED.**
  Web `checks`/`cooling`/`sanitizer`/`temp-log` (HACCP) and
  `receiving`/`receiving-matches` write a `sync_feed` `appendOp` in-transaction
  for cross-host replication; no native repo does. **Ratified** as an edge-blocker
  for receiving + receiving-matches (`lariat-native-edge-blockers.md`). For the
  **four HACCP routes** the omission is **NOT** being ratified-away — once C5
  deletes a web write route, native-originated writes feed no sync_feed at all,
  so cross-device replication of food-safety data silently stops.
  **OWNER DECISION 2026-07-07: keep these 4 routes BLOCKED from C5 deletion until
  a native `sync_feed` producer exists** (port the in-tx `appendOp` into
  LineCheck/Cooling/Sanitizer/TempLog repositories) **or** the deployment is
  confirmed single-device-system-of-record and the block is then explicitly
  lifted. Do not delete `checks`/`cooling`/`sanitizer`/`temp-log` in any C5 wave
  until that gate clears.
- **`reservations` guest PII** is an open read on **both** web and native (no PIN
  either side) — a web-side design choice, not a port regression. Owner note.
- **Audit payload-shape divergences** (non-blocking): performance-reviews uses
  `punctuality_score`/… keys vs web `punctuality`/…; tip-pool/wage-notices wrap
  the row as `{row:{…}}` vs a bare row. Data present, keyed/nested differently.
- **Sound has no native `updateScene`** (web has a PATCH scene endpoint) — a
  missing feature, not a security gap.

## ✅ Verify-refuted ported claims — ALL RESOLVED 2026-07-07

> **Re-verified against `main` 397af37 (2026-07-07).** The three rows below were
> the 2026-07-03 verify snapshot; none still block C5:
> - **`app/api/beo/route.js`** — RESOLVED (already fixed in a later BEO wave, not
>   in this one). `deleteEvent` and `setPrepDone` both now carry `AND location_id = ?`
>   (`BeoBoardRepository.swift` :425 / :398) with corrected comments, and cross-location
>   regression tests exist (`testDeleteEventDoesNotDeleteForeignLocationEvent`,
>   `testSetPrepDoneDoesNotTouchForeignLocationTask`). The 2026-07-03 line/column
>   numbers below are stale.
> - **`app/api/breaks/route.js`** — FIXED 2026-07-07 (`feat/lariat-native-c1-break-waived-parity`).
>   The open-break 409 guard now runs unconditionally (waived meals included), matching
>   web; the previously-untested waived branch got coverage (waived-while-open 409,
>   single-completed-row shape, the two 400s).
> - **`app/api/kitchen-assistant/route.js`** — RECLASSIFIED (no code change): `code_search`
>   and `db_query` are correct **documented deferred stubs** (`KitchenAssistantEngine.swift`
>   :15-16, :171-184); web env-gates both off by default, so no production audit/write rule
>   is lost. The row was a *classification* error ("ported" → **deferred/edge**), not a
>   ported-write defect. The route's ten mutating actions are ported + tested in
>   `AssistantActionRepository`/`AssistantActionRepositoryTests` (43 tests).
>
> Detail rows retained below as the historical verify record.

| Route | Native owner | Gaps found (2026-07-03 snapshot — see resolution note above) |
|---|---|---|
| `app/api/beo/route.js` | LariatNative/Sources/LariatDB/BeoBoardRepository.swift (load, createEvent, updateEvent, addLine, updateLine, deleteLine, addPrepTask, setPrepDone, deleteEvent — audit_events in the SAME transaction via AuditedWriteRunner per file header) | delete_event loses location scoping: web deletes with 'WHERE id = ? AND location_id = ?' (app/api/beo/route.js:425) but native deleteEvent runs 'DELETE FROM beo_events WHERE id = ?' with no location predicate (LariatNative/Sources/LariatDB/BeoBoardRepository.swift:421) — a caller scoped to location A can delete location B's event, and the FK cascades then wipe that event's beo_line_items and beo_prep_tasks; no test covers cross-location delete_event and no divergence is documented; prep_done loses location scoping: web updates with 'WHERE id = ? AND location_id = ?' (app/api/beo/route.js:407) but native setPrepDone runs 'UPDATE beo_prep_tasks SET done = ? WHERE id = ?' (LariatNative/Sources/LariatDB/BeoBoardRepository.swift:396-398) under a comment that misstates the web ('Web parity: no location scope on this UPDATE') — cross-location prep-task toggling is possible and the divergence is documented incorrectly as parity, not as deliberate; note: everything else verified faithful — event/update_event COALESCE + min_spend provided-flag CASE (create/patch/clear/negative-reject all tested), line/update_line/delete_line parent-event-subquery scoping (tested in both hijack directions), 422 malformed course_id before any write (tested with zero-audit assertion), FK ON DELETE CASCADE + PRAGMA foreign_keys=1 (tested), audit rows in the same transaction enforced by AuditEventWriter.post's isInsideTransaction guard |
| `app/api/breaks/route.js` | LariatNative/Sources/LariatDB/BreakRepository.swift (start at :65, end at :144, load at :14) + LariatModel/Compute/BreakCompute.swift (COMPS #39 evaluateShift port incl. waived-meal handling) | Open-break 409 guard is skipped for waived entries: web runs the open-break check unconditionally before any insert — a waived-meal POST while the cook has an open break returns 409 (app/api/breaks/route.js:88-98 executes before the waived-row construction at :104) — but native only performs the check when waived == 0 (LariatNative/Sources/LariatDB/BreakRepository.swift:93-105), so a waived-meal row inserts successfully alongside an open break; undocumented and untested; Cited test does not cover the claimed waived-meal action: no test in BreakRepositoryTests.swift passes waived: true — the 'waived meal stored as single completed row (ended_at=started_at, duration_min 0)' rule (implemented at BreakRepository.swift:89-90), the 'only meal breaks can be waived under COMPS #39' 400 (:69-71), and the 'waived without waiver_ref' 400 (:72-74) are all implemented but have zero native test coverage; the file contains only testStartAndEndWithAudit, testOpenBreak409, testEndCrossLocationNotFound; Cited BreakComputeTests does not cover 'incl. waived-meal handling': LariatNative/Tests/LariatModelTests/BreakComputeTests.swift has only 3 tests (requiredRestBreaks, requiresMealBreak, evaluateShiftOwedRest) — no waived-meal, short-break, or open-break-warning case is exercised, so the waived branch of the evaluateShift port (BreakCompute.swift:84-97, 111-123 — itself a faithful line-for-line port of lib/breaks.ts) is unverified; note: end-break read+math+UPDATE atomicity, 400 ended_at<=started_at, GET location+shift_date+cook_id filtering with evaluateShift only when cook_id and both shift bounds present — all verified faithful |
| `app/api/kitchen-assistant/route.js` | LariatNative/Sources/LariatDB/KitchenAssistantEngine.swift | REFUTED — claimed action 'code_search read action posts a view audit_event' is NOT ported: web posts an in-transaction audit_events action='view' row on successful code_search (route.js:383-406); native returns a soft 'code_search isn't available on this device yet' stub with NO audit write (KitchenAssistantEngine.swift:171-177). Mitigation: deferral is documented ('Phase B plan', engine doc lines 15-18) and web code_search is env-gated off by default (lib/devCodeSearch.ts:113, LARIAT_DEV_CODE_SEARCH), so no production-default audit rule fires on web either — but the ledger's 'ported' classification for this action is false; REFUTED — claimed action 'db_query' is NOT ported: native soft-stubs it (KitchenAssistantEngine.swift:178-184); web's tiered vetted-query runner + row-cap + LLM summary path (route.js:408-441, lib/dbQueryTool) is lost to native users if Phase C5 deletes this route. Read-only (no write rule), but it is a claimed action of a route marked fully ported; citation gap: the ten mutating action branches live in AssistantActionRepository.swift (not the cited KitchenAssistantEngine.swift) and are tested in AssistantActionRepositoryTests.swift (43 tests), not the cited KitchenAssistantEngineTests.swift — native coverage exists and is thorough, but the cited owner/test pair alone does not cover the route's write rules; verified (no gaps): message required/≤2000→400, deterministic Q-vs-C classifier + PIN-before-LLM #248 hard gate (engine:89-102, testUnpinnedWriteCommandBlocksAndSkipsOllama), per-action PIN allowlist identical to route.js:462-472 (AssistantActionRepository.pinRequiredActions), #247 cook-tier redaction (AssistantContextRepository hasPin gating, testCookTierPromptCarriesRedactionSentinels), server-authoritative calculator + FDA temp/receiving validators with throw→'fail'-never-'na' (haccpStatusAndNote), Number.isFinite soft-rejects (delta/qty/stars/multiplier), cross-location beo_add_prep guard + huge-event_id no-trap, roster validation with empty/missing-table fallbacks, every write in one transaction with in-transaction audit_events insert (AuditedWriteRunner + AuditEventWriter.isInsideTransaction), undo metadata returned, question-path JSON strip + only-JSON apology, conversation store/sweep, generic no-leak actionError, 502 LLM / 500 context mapping |

## Low-confidence rows (human review)

| Route | Class | Why |
|---|---|---|
| `app/api/cleaning-schedule/route.js` | edge-retained | UNPORTED WRITE GAP filed here only because no ledger bucket fits: live caller app/admin/cleaning-schedule/CleaningScheduleEditor.jsx (not dead), native only READS cleaning_schedule (CommandRepository.swift:160 command-summary aggregate — no native CRUD, so not ported), and the route does NOT appear in lariat-native-edge-blockers.md (so edge retention is de facto, not ratified). Needs either a native schedule-admin port (C5 wave 2 'cleaning') or an explicit edge-blocker entry. |
| `app/api/compute/status/route.js` | edge-retained | The snapshot-writing compute engine has NO native owner (no INSERT INTO margin_snapshots/accounting_variance anywhere in LariatNative/Sources; A4 spec gap-audit ports only the read-side computeCostVariance card) and native CostingRepository READS accounting_variance, so the web engine remains the sole producer — it cannot be dead (also auto-triggered by app/api/receiving/route.js and covered by tests/js/test-compute-status-route.mjs). It is NOT listed in lariat-native-edge-blockers.md, so this is a genuine C1 gap: engine + trigger stay on the edge today but lack a ratified blocker entry or a native port plan — needs human review. |
| `app/api/locations/route.js` | edge-retained | NOT ported: no INSERT/UPDATE against the locations table exists anywhere in LariatNative/Sources. Not dead: live callers at app/setup/page.jsx (first-run venue naming POST) and app/_components/Sidebar.jsx (GET), plus tests/js/test-setup-flow.mjs. It is also NOT listed in docs/superpowers/specs/lariat-native-edge-blockers.md — classified edge-retained as web first-run-setup machinery, but this is a judgment call: it needs a human decision to either port venue seed/rename to native or ratify it in the blocker log. |
| `app/api/performance-reviews/[id]/route.ts` | edge-retained | PORT GAP flagged for human review, not a ratified edge blocker: native PerformanceReviewsRepository.swift has only list/create — no delete — and no LariatNative test touches performance_reviews deletion; the route has a live caller (app/management/performance-reviews/PerformanceReviewBoard.tsx:126) so it is not dead, and the edge-blockers spec does not cover it. It de facto stays on the web edge until a native delete (ideally adding the missing audit) lands. |
| `app/api/preshift-notes/route.ts` | edge-retained | PORT GAP flagged for human review, not a ratified edge blocker: no native writer for preshift_notes exists anywhere in LariatNative — the only native touch is a read-only COUNT for the command rollup (LariatNative/Sources/LariatDB/CommandRepository.swift:167); the route has a live caller (app/_components/PreshiftNotes.jsx:53 on the Today page) so it is not dead, and it is absent from the edge-blockers spec. The NULL-tolerant upsert rules stay web-owned until a native port lands. |
| `app/api/recipes/[slug]/photos/[id]/route.js` | edge-retained | FORCED-CHOICE PLACEHOLDER — this is a live, unported write path that fits none of the ledger categories cleanly: no native owner (zero photo hits in LariatNative), NOT listed in lariat-native-edge-blockers.md, and not dead (live callers RecipePhotoStrip.jsx / RecipePhotoUploader.jsx under app/recipes/[slug]/ plus tests/js/test-recipe-photos-api.mjs). Needs a human decision: port to native (with the on-disk photo store) or ratify a new edge-blocker entry. |
| `app/api/recipes/[slug]/photos/route.js` | edge-retained | FORCED-CHOICE PLACEHOLDER — same C1 gap as the sibling item route: live callers in app/recipes/[slug]/edit (RecipePhotoUploader) and no native owner anywhere in LariatNative, but also absent from the edge-blockers spec, so 'edge-retained' is not a ratified decision. The file-on-disk store + multipart handling make it a distinct porting problem; flag for human review. |
| `app/api/recipes/[slug]/route.js` | edge-retained | GAP ROUTE: live write (called by app/recipes/[slug]/edit/RecipeEditForm.jsx) with NO native writer — LariatNative only reads recipes.json (DishBridgeRecipeLoader) and checks entities_recipes slug collisions (SpecialsRepository); it is also absent from lariat-native-edge-blockers.md. Classified edge-retained as the least-destructive bucket, but it is really an unported write needing a port decision or a blocker-log entry. |
| `app/api/service-hours/route.js` | edge-retained | GAP ROUTE: live write (called by app/admin/service-hours/ServiceHoursEditor.jsx) with zero native trace — grep for service_hours/ServiceHours/opens_at across LariatNative Sources+Tests returns nothing — and it is not in lariat-native-edge-blockers.md. Classified edge-retained as the least-destructive bucket; really an unported write needing a port decision or a blocker-log entry (plus a missing-audit follow-up). |

## ported (71)

| Route | Methods | Native owner | Parity test | Rules | V |
|---|---|---|---|---|---|
| `app/api/allergens/attestations/route.js` | GET POST | LariatNative/Sources/LariatDB/AllergenAttestationRepository.swift | LariatNative/Tests/LariatDBTests/AllergenAttestationRepositoryTests.swift | GET cook-readable (no PIN), ?slug 404 on unknown recipe, no-store; POST requirePin + withIdempotency, 400 invalid JSON/missing slug/missing attested_by/non-array allergens, 404 unknown slug, 201 on success; append-only insert with in-transaction audit_events (entity=allergen_attestation, action=insert, actor_source=manager_ui); location via locationFromRequest/locationFromBody. | ✓ |
| `app/api/auth/manager-pins/route.js` | GET POST PATCH DELETE | LariatNative/Sources/LariatDB/ManagerPinRepository.swift | LariatNative/Tests/LariatDBTests/ManagerPinRepositoryTests.swift | All methods requirePin; POST/PATCH/DELETE wrapped in withIdempotency (replay dedupe = one audit row per real edit); 422 on invalid JSON or validation failure (echoes err.message), 200 on success; every write runs in getDb().transaction with postAuditEvent(entity=manager_pin_user, actor_source=manager_ui) in the same transaction; location via locationFromBody/locationFromRequest. | ✓ |
| `app/api/auth/temp-pin/issue/route.js` | POST | LariatNative/Sources/LariatDB/TempPinRepository.swift | LariatNative/Tests/LariatDBTests/TempPinRepositoryTests.swift | requirePin; deliberately NOT withIdempotency (raw PIN in response must not be cached in idempotency_keys — audit 2026-05-08 Tier-1 HIGH #2); 422 validation (label required ≤200, expires_at canonical ISO-8601 UTC and future, ≥1 known scope, pin_length clamped to 4..PIN_MAX); UNIQUE pin_hash collision retried ×5 then 503; stores SHA-256(pin) only, raw PIN returned exactly once; audit_events insert (entity=temp_pin, actor_source=manager_ui) in same transaction; locationFromBody. | ✓ |
| `app/api/auth/temp-pin/revoke/route.js` | POST | LariatNative/Sources/LariatDB/TempPinRepository.swift | LariatNative/Tests/LariatDBTests/TempPinRepositoryTests.swift | requirePin + withIdempotency; 422 unless id is a positive integer; location-scoped existence check (id AND location_id) → 404 not found; idempotent by construction — already-revoked returns existing revoked_at with NO second audit row; UPDATE + audit_events (entity=temp_pin, action=update, actor_source=manager_ui, payload {revoked, revoked_at}) in one transaction; 200 {id, revoked_at}. | ✓ |
| `app/api/beo/courses/[id]/route.js` | PATCH DELETE | LariatNative/Sources/LariatDB/BeoCoursesRepository.swift (patch at :124, delete at :225) | LariatNative/Tests/LariatDBTests/BeoCoursesRepositoryTests.swift (testPatchUpdatesFireAtAndWritesAuditRow, testPatchClearsAndSetsStation, testPatchRejectsEmptyCourseLabel, testDeleteRemovesCourseAndSetsLineCourseIdNullViaFk) | Gate: master PIN OR temp-PIN with scope beo.fire_at_edit (401); 400 bad id; 422 invalid JSON / empty course_label / non-canonical ISO-8601-UTC fire_at / negative sort_order / non-slug station_id; 404 location-scoped lookup (locationFromBodyOrRequest); UPDATE (COALESCE + provided-flag CASE for notes/station) or DELETE + audit_events (entity beo_course, actor_source manager_ui) in same db.transaction; withIdempotency; 500 tx failure. | ✓ |
| `app/api/beo/courses/route.js` | GET POST | LariatNative/Sources/LariatDB/BeoCoursesRepository.swift (create at :53, list at :31) | LariatNative/Tests/LariatDBTests/BeoCoursesRepositoryTests.swift (testCreateCoursePersistsAndWritesAuditRow, testCreateAppendsSortOrderOnSecondCourse, testCreateThrowsNotFoundForEventInAnotherLocation, testCreateRejectsNonCanonicalFireAtBeforeAnyWrite) | GET and POST both gated master PIN OR temp-PIN scope beo.fire_at_edit (401 — fire-schedule rollup is the separate public read); GET 422 bad event_id; POST 422 invalid JSON / event_id / validateCoursePayload failure, 404 event not at location (blocks cross-location course creation); INSERT + audit_events (beo_course/insert, manager_ui) in same db.transaction; withIdempotency; 500 tx failure. | ✓ |
| `app/api/beo/route.js` | GET POST | LariatNative/Sources/LariatDB/BeoBoardRepository.swift (load, createEvent, updateEvent, addLine, updateLine, deleteLine, addPrepTask, setPrepDone, deleteEvent — audit_events in the SAME transaction via AuditedWriteRunner per file header) | LariatNative/Tests/LariatDBTests/BeoBoardRepositoryTests.swift (per-action audit-row tests, min_spend create/patch/clear/negative-reject, location-scoping incl. line writes via parent-event subquery, FK delete cascades, malformed course_id rejected before any write) | GET requirePin (401); POST per-body gate: a course_id-only update_line accepts temp-PIN scope beo.fire_at_edit (allowed-keys whitelist is the boundary), any other field requires master PIN cookie (401); 400 missing title/id/event_id+item_name, negative min_spend, unknown action; 422 malformed course_id patch; partial-patch COALESCE + provided-flag CASE semantics (min_spend explicitly clearable); every mutation pairs with an in-transaction audit_events row (actor_source 'api'); line writes/deletes location-scoped via parent-event subquery; delete_event relies on FK ON DELETE CASCADE; withIdempotency on POST (native deliberately has no idempotency layer — asserted by testCreateEventHasNoIdempotencyLayer; treated as an HTTP-transport concern). | ✓ |
| `app/api/breaks/route.js` | POST PATCH GET | LariatNative/Sources/LariatDB/BreakRepository.swift (start at :65, end at :144, load at :14) + LariatModel/Compute/BreakCompute.swift (COMPS #39 evaluateShift port incl. waived-meal handling) | LariatNative/Tests/LariatDBTests/BreakRepositoryTests.swift (testStartAndEndWithAudit, testOpenBreak409, testEndCrossLocationNotFound) + LariatModelTests/BreakComputeTests.swift | No PIN gate; POST: 400 kind not meal\|rest / missing cook_id / non-ISO started_at / waived non-meal ('only meal breaks can be waived under COMPS #39') / waived without waiver_ref, 409 cook already has an open break, waived rows stored completed (ended_at=started_at, duration_min 0); PATCH: 400 bad id or ended_at<=started_at, 404 unknown break AND cross-location break (IDOR guard, existence not leaked), 409 already ended, atomic read+duration-math+UPDATE in one transaction; both writes post audit_events (shift_breaks, actor_source cook_ui) in-transaction; withIdempotency on POST/PATCH; GET filters location_id+shift_date (+optional cook_id) and runs evaluateShift when shift bounds given. | ✓ |
| `app/api/certifications/route.js` | GET POST PATCH | LariatNative/Sources/LariatDB/StaffCertRepository.swift | LariatNative/Tests/LariatDBTests/StaffCertRepositoryTests.swift | PIN gate pic.staff_certs when pinRequiredForPic (403); cert_type must be in {cfpm,food_handler,tips,allergen,other}, cook_id+cert_label required, issued_on/expires_on must match YYYY-MM-DD (400); PATCH 404 on unknown id, 400 when nothing to update; clip-length truncation (64/120/300); in-transaction audit_events (insert/update, actor_source pic_ui); withIdempotency on POST/PATCH; POST location from body, PATCH keeps existing row's location_id (immutable), GET filtered by location_id (+optional cook_id). | ✓ |
| `app/api/checks/route.ts` | GET POST | LariatNative/Sources/LariatDB/LineCheckRepository.swift | LariatNative/Tests/LariatDBTests/LineCheckRepositoryTests.swift | shift_date/station_id/item/cook_id required, status must be pass\|fail\|na (400); glove_change_attested tri-state 1/0/NULL (NULL = non-RTE item, stays out of attestation accounting); HACCP-regulated: INSERT + in-tx audit_events (actor cook_ui) + in-tx sync_feed appendOp (rowJson excludes id, op_id is cross-host idempotency key); withIdempotency on POST; location_id from body; GET filtered by location_id + optional date/station, LIMIT 500. | ✓ |
| `app/api/cleaning/route.ts` | GET POST | LariatNative/Sources/LariatDB/CleaningRepository.swift | LariatNative/Tests/LariatDBTests/CleaningRepositoryTests.swift | validateCleaningLog (lib/cleaning.ts): body must be object, item-or-task required non-empty, type/length checks → 400 with specific reason; defaults shift_date=todayISO, area='General', task='Cleaning', completed_at=now; in-transaction audit_events insert (actor = cook_id \|\| verified_by_cook_id, actor_source cook_ui); withIdempotency on POST; location_id from body; GET scoped by location_id + shift_date (default today). | ✓ |
| `app/api/cooling/route.js` | POST PATCH GET | LariatNative/Sources/LariatDB/CoolingRepository.swift | LariatNative/Tests/LariatDBTests/CoolingRepositoryTests.swift | No PIN (cook UI); POST/PATCH withIdempotency; POST 400 via validateCoolingStart, clips (item 200, station/cook 64, at 40); PATCH 400 bad id/decision, 404 unknown id AND cross-location IDOR (deliberately 404 not 403), 422 breach without corrective_action (max 500 chars), SELECT+classify+UPDATE+audit in ONE transaction; audit_events in-transaction both paths; sync_feed appendOp (transport input) in same transaction; GET location-scoped (?location), open + optional closed-by-date. | ✓ |
| `app/api/costing/ingredient-masters/route.js` | GET PATCH | LariatNative/Sources/LariatDB/IngredientMastersRepository.swift | LariatNative/Tests/LariatDBTests/IngredientMastersRepositoryTests.swift | PIN-gated (middleware + defensive in-route requirePin) on GET+PATCH; PATCH withIdempotency; 400 invalid JSON/missing master_id/empty updates, 422 per-field type/emptiness violations (canonical_name non-empty ≤200, category ≤80, vendor ≤80, lock_reason ≤80, quality_locked boolean-ish, last_reviewed null\|'now'\|ISO) and MasterUpdateRejectedError, 404 master not found; location from body>request>'default', actor_source='manager_ui'; audit_events in-transaction with the UPDATE; GET filter set {all,needs_review,reviewed}, limit clamp 1-1000. | ✓ |
| `app/api/costing/pack-changes/route.js` | GET POST | LariatNative/Sources/LariatDB/PackChangesRepository.swift | LariatNative/Tests/LariatDBTests/PackChangesRepositoryTests.swift | PIN-gated (middleware /api/costing matcher + in-route requirePin) on GET+POST; POST withIdempotency AND domain-idempotent (already-acknowledged returns was_already_acknowledged=true, no second audit); 400 invalid JSON/non-positive id, 404 row not found; note clipped to 500; audit is JSONL (NOT in-transaction audit_events) and deliberately non-blocking — audit failure after DB commit is logged, still 200; no location_id scoping; GET filter {open,acknowledged,all}, vendor filter, limit clamp 1-1000. | ✓ |
| `app/api/date-marks/route.js` | POST PATCH GET | LariatNative/Sources/LariatDB/DateMarkRepository.swift | LariatNative/Tests/LariatDBTests/DateMarkRepositoryTests.swift | No PIN (cook UI); POST/PATCH withIdempotency; POST 400 via validateDateMarkCreate + belt-and-suspenders null re-check, clips (item 200, batch_ref 120, cook 64, prepared_on 10), location from body; PATCH 400 missing id/reason or reason not in {expired,early_use,quality,contamination}, 404 unknown, 409 already discarded (pre-check+UPDATE+audit atomic in one transaction); audit_events in-transaction both paths (actor_source='cook_ui'); GET active marks + expiring scan, location-scoped; NOTE: web PATCH has NO cross-location guard (native discard adds one — stricter). | ✓ |
| `app/api/dining-tables/[id]/route.js` | PATCH DELETE | LariatNative/Sources/LariatDB/DiningTablesRepository.swift (update L121, delete L195; header cites parity with this route's PATCH/DELETE) | LariatNative/Tests/LariatDBTests/DiningTablesRepositoryTests.swift (testStatusTransitionWritesAuditWithFromTo, testPatchNoChangeThrows, testPatchOtherLocationThrowsNotFound, testDeleteRemovesRowAndWritesDeleteAudit, testDeleteOtherLocationThrowsNotFoundAndRowSurvives, testCapacityUpdateOutOfRangeThrows) | PATCH: 400 bad id/bad status/capacity not 1..50/no-change; 404 if row not at caller location (WHERE id AND location_id); UPDATE+in-transaction audit_events (entity dining_tables, action update, from/to status payload); withIdempotency; loc from body. DELETE: loc from ?location query, 404 on 0 changes, DELETE+in-transaction audit (action delete); withIdempotency. | ✓ |
| `app/api/dining-tables/route.js` | GET POST | LariatNative/Sources/LariatDB/DiningTablesRepository.swift (create L67, list L24, isPrimaryKeyConflict L235) | LariatNative/Tests/LariatDBTests/DiningTablesRepositoryTests.swift (testCreateWritesRowAndInsertAudit, testCreateRejectsCapacityZeroAndFiftyOne, testCreateRejectsBadStatus, testCreateDuplicateThrows409AndOtherLocationSucceeds, testListScopesByLocation) | GET: location-scoped list ORDER BY id ASC. POST: 400 missing id(<=32)/name(<=100), capacity 1..50 default 2, status enum default open, x/y/w/h numeric defaults, notes<=500; INSERT+in-transaction audit_events (action insert); 409 on PK/UNIQUE conflict per (location_id,id); withIdempotency; loc from body. | ✓ |
| `app/api/dish-components/route.ts` | GET POST DELETE | LariatNative/Sources/LariatDB/DishComponentsRepository.swift (upsert L74, delete L156, list L39; header cites parity with this route's GET/POST/DELETE and notes the un-scoped DELETE) | LariatNative/Tests/LariatDBTests/DishComponentsRepositoryTests.swift (testUpsertInsertsCanonicalRow, testUpsertIdenticalRowSkipped, testUpsertChangedQtyUpdatesInPlace, testVendorUpsertDedupesOnIngredient, testValidationFailureThrowsBeforeWrite, testDeleteRemovesRowById, testDeleteRejectsNonPositiveId, testWritesPostNoAuditEvents) | POST: 400 via validateDishComponent or dish_name normalizing to empty; dish_name canonicalized (normalizeDishName), clips recipe_slug 80/vendor_ingredient 200/unit 24/notes 500; transactional upsert with identical-row skip shared with CLI importer; NO audit_events; withIdempotency; loc from body. DELETE: 400 non-positive id, delete by id with NO location scoping, missing id silently ok. GET: location-scoped, optional ?dish normalized filter. | ✓ |
| `app/api/eighty-six/resolve/route.ts` | POST | LariatNative/Sources/LariatDB/EightySixRepository.swift (resolve L92: snapshot, cross-location guard, UPDATE + audit in one transaction) | LariatNative/Tests/LariatDBTests/EightySixRepositoryTests.swift (testAddAndResolveWithAudit, testResolveCrossLocationReturnsNotFound, testDoubleResolve409) | 400 non-positive id; caller location from ?location= (locationFromRequest, never body — post-2026-05-08 IDOR fix); row snapshotted then cross-location mismatch returns 404 (not 403, no existence leak); 409 if already resolved (entry echoed); UPDATE resolved_at/resolved_by + in-transaction audit_events (entity eighty_six, action update, note 'resolved', shift_date + row location); withIdempotency. | ✓ |
| `app/api/eighty-six/route.ts` | GET POST | LariatNative/Sources/LariatDB/EightySixRepository.swift (add L50, load L16) with cascade compute in LariatNative/Sources/LariatModel/Compute/SubRecipeCascade.swift | LariatNative/Tests/LariatDBTests/EightySixRepositoryTests.swift (testAddAndResolveWithAudit, testEmptyItemRejected) | GET: date defaults todayISO, ?all=1 includes resolved, location-scoped, returns active rows + cascaded items via subRecipeGraph. POST: 400 missing item (clip 300); clips shift_date/station_id/kind(default 'item')/reason/quantity/cook_id; INSERT + in-transaction audit_events (entity eighty_six, action insert); withIdempotency; loc from body. | ✓ |
| `app/api/equipment/maintenance/route.ts` | GET POST | LariatNative/Sources/LariatDB/EquipmentRepository.swift (addMaintenance L149, listMaintenance L54) | LariatNative/Tests/LariatDBTests/EquipmentRepositoryTests.swift (testAddMaintenanceValidationsAndDefaults, testMaintenanceListFiltersByEquipmentAndOrders, testMaintenanceForMissingEquipmentFailsFK) | GET: location-scoped, optional ?equipment_id filter, ORDER BY service_date DESC, id DESC. POST: 400 missing/invalid equipment_id or service_date; type defaults 'Routine', cost via toMoney, notes<=1000, receipt_reference<=500; single INSERT, NO audit_events (web parity noted in the Swift repo header), no explicit transaction; withIdempotency; loc from body. | ✓ |
| `app/api/equipment/parts/route.ts` | GET POST | LariatNative/Sources/LariatDB/EquipmentRepository.swift (addPart L182, listParts L72) | LariatNative/Tests/LariatDBTests/EquipmentRepositoryTests.swift (testAddPartValidationsAndListOrdering, testDuplicateAddsCreateDuplicateRows) | GET: location-scoped, optional ?equipment_id filter, ORDER BY equipment_id, part_number. POST: 400 missing/invalid equipment_id or part_number(<=500); clips description/vendor/last_order_ref<=500, notes<=2000, last_ordered<=32; unit_price/qty_on_hand via toNum; single INSERT, NO audit_events, no dedupe (duplicate adds create duplicate rows); withIdempotency; loc from body. | ✓ |
| `app/api/equipment/route.ts` | GET POST | LariatNative/Sources/LariatDB/EquipmentRepository.swift (addEquipment L109, listEquipment L33 incl. the SUM(cost) maintenance_cost aggregate) | LariatNative/Tests/LariatDBTests/EquipmentRepositoryTests.swift (testAddEquipmentAppliesDefaultsAndWritesNoAudit, testAddEquipmentRequiresNameAndWritesNothing, testAddEquipmentClipsLongFields, testAddEquipmentIsLocationScoped, testListEquipmentAggregatesMaintenanceCostAndOrders) | GET: location-scoped list ORDER BY category, name with correlated maintenance_cost = COALESCE(SUM(cost)) subquery. POST: 400 missing name(<=200); category default 'Uncategorized'(<=60), status default 'active'(<=32), text fields<=500, notes<=2000, purchase_cost via toMoney; single INSERT, NO audit_events; withIdempotency; loc from body. | ✓ |
| `app/api/equipment/schedule/route.ts` | GET POST | LariatNative/Sources/LariatDB/EquipmentRepository.swift (addSchedule, line 218; listSchedule, line 90) | LariatNative/Tests/LariatDBTests/EquipmentRepositoryTests.swift (testAddScheduleValidationsAndNullsLastOrdering, line 202) | GET: location_id filter + optional equipment_id, ORDER BY next_due nulls-last; POST: idempotent (withIdempotency), no PIN, requires integer equipment_id>0 + task(clip 500) + frequency(clip 60) else 400, clips last_done/next_due(32)/notes(2000), location from body, single INSERT .run(), NO audit (neither audit_events nor JSONL), 500 on failure. | ✓ |
| `app/api/gold-stars/[id]/route.ts` | DELETE | LariatNative/Sources/LariatDB/GoldStarsRepository.swift (remove, line 126) | LariatNative/Tests/LariatDBTests/GoldStarsRepositoryTests.swift (testRemoveSoftDeletesAndAudits line 169, testRemoveMissingOrWrongLocationIsNotFound line 191, testSecondRemoveIsNotFoundNoIdempotency line 223) | DELETE: PIN-gated (requirePin) then idempotent (withIdempotency); integer id>0 else 400; location-scoped lookup, 404 if missing or already soft-deleted; db.transaction wrapping UPDATE (soft delete, deleted_by='manager_pin') + in-transaction audit_events row (action='delete', payload snapshot of the star); 200 {ok,id}. | ✓ |
| `app/api/gold-stars/route.ts` | GET POST | LariatNative/Sources/LariatDB/GoldStarsRepository.swift (award line 83, board line 37, leaderboard line 58) | LariatNative/Tests/LariatDBTests/GoldStarsRepositoryTests.swift (testAwardWritesRowAndAuditInOneTransaction line 35, testAwardClampsStars line 78, testBoardShowsTodayAndHidesYesterday line 91) | GET: location-scoped; default view = today-only board (localtime day, deleted_at IS NULL, LIMIT 50 id DESC); ?view=leaderboard = all-time per-cook SUM/COUNT/MAX. POST: PIN-gated + idempotent; requires non-empty cook_name + reason else 400; stars clamped to 1-3; db.transaction INSERT + in-transaction audit_events (action='insert'); generic 500 on error. | ✓ |
| `app/api/host/waitlist/[id]/route.js` | PATCH | LariatNative/Sources/LariatDB/HostWaitlistRepository.swift (transition, line 102) | LariatNative/Tests/LariatDBTests/HostWaitlistRepositoryTests.swift (testTransitionToSeatedStampsSeatedAt line 132, testReTransitioningSeatedPartyThrows409 line 152, testWritesLogJsonlAndNoAuditEventsRows line 167) | PATCH: PIN-gated then idempotent; integer id>0 else 400; status must be 'seated'\|'left' else 400; isValidStatusTransition gate else 409; 404 unknown party; notes clipped 500 and COALESCEd; transaction UPDATE stamping seated_at/left_at + updated_at; JSONL file-stream audit via logAuditAction (deliberately NO audit_events row); lookup NOT location-filtered (location taken from the row); returns updated party. | ✓ |
| `app/api/host/waitlist/route.js` | GET POST | LariatNative/Sources/LariatDB/HostWaitlistRepository.swift (addParty line 62, load line 36) + LariatNative/Sources/LariatModel/Compute/HostStandCompute.swift (sanitize/summarize) | LariatNative/Tests/LariatDBTests/HostWaitlistRepositoryTests.swift (testAddInsertsPartyAndReturnsRow line 79, testAddRejectsMissingPartyName line 61, testLoadFiltersToActiveAndTodayRows line 28) + LariatNative/Tests/LariatModelTests/HostStandComputeTests.swift | GET: PIN-gated; location via ?location else DEFAULT_LOCATION_ID; returns waiting + today's seated/left (UTC date prefix on seated_at/left_at) ordered by joined_at, plus summarizeWaitlist summary. POST: PIN-gated + idempotent; sanitizeWaitlistInput requires party_name + party_size>0 else 400; location from body else default; transaction INSERT + JSONL audit (logAuditAction, no audit_events); 201 with inserted row; 500 on error. | ✓ |
| `app/api/inventory/counts/[id]/lines/route.js` | POST | LariatNative/Sources/LariatDB/InventoryCountRepository.swift (upsertLine, line 105) + LariatNative/Sources/LariatModel/Compute/IngredientKey.swift | LariatNative/Tests/LariatDBTests/InventoryCountRepositoryTests.swift (testUpsertLineInsertsThenUpsertsSameSlot line 66, testCanonicalizesCapitalizationDedup line 83, testNoSkuUpsertsByEmptyString line 98, testUpsertLineOnClosedCountThrows line 140) | POST: idempotent, no PIN; count id integer>0 else 400; ingredient required and canonicalized via normalizeIngredientKey else 400; sku NULL coerced to '' so UNIQUE(count_id,ingredient,sku) dedups; transaction: 404 count-not-found, 409 count-closed, UPSERT with RETURNING id (lastInsertRowid unreliable on conflict), in-transaction audit_events with action='update' for both branches (CHECK constraint has no 'upsert'); location from body; 500 on error. | ✓ |
| `app/api/inventory/counts/[id]/route.js` | GET PATCH | LariatNative/Sources/LariatDB/InventoryCountRepository.swift (closeCount line 179, reopenCount line 201, getCount line 50) | LariatNative/Tests/LariatDBTests/InventoryCountRepositoryTests.swift (testCloseThenReopenTogglesAndAuditsThree line 154, testCloseUnknownCountThrowsNotFound line 175, testGetCountReturnsHeadAndLinesOrdered line 226) | GET: integer id>0 else 400, location-scoped head + lines (ordered by ingredient), 404 if missing. PATCH: idempotent, no PIN; exactly one of close\|reopen booleans else 400 'no action'; transaction: location-scoped lookup 404, close on already-closed → 409, reopen unconditional NULL; each transition writes in-transaction audit_events (action='update', payload.transition); location from body on write. | ✓ |
| `app/api/inventory/counts/route.js` | GET POST | LariatNative/Sources/LariatDB/InventoryCountRepository.swift (openCount line 74, listCounts line 27) | LariatNative/Tests/LariatDBTests/InventoryCountRepositoryTests.swift (testOpenCountPersistsHeaderAndAudits line 35, testOpenCountDefaultsCountDateToToday line 53, testListScopesByLocationAndOrders line 186, testListOpenOnlyFilter line 213) | GET: location-scoped, last 50 by opened_at DESC with same-location line_count subquery, ?open=1 filters closed_at IS NULL; 500 on error. POST: idempotent, no PIN; count_date defaults todayISO(), label clipped 100, cook_id clipped 64, location from body; transaction INSERT + in-transaction audit_events (action='insert', payload {count_date,label}); returns {ok,id}. | ✓ |
| `app/api/inventory/par/route.js` | GET POST DELETE | LariatNative/Sources/LariatDB/InventoryParRepository.swift | LariatNative/Tests/LariatDBTests/InventoryParRepositoryTests.swift | POST: ingredient required else 400, clip caps (ingredient 200 / sku 80 / vendor 120 / note 500), insert-or-update keyed (location_id,ingredient,sku); DELETE: integer id>0 else 400, 404 if row not at caller's location; both wrapped in db.transaction with in-transaction audit_events (insert/update/delete, actor_source 'api'); withIdempotency on POST+DELETE; all reads/writes location_id-scoped; 500 on error. | ✓ |
| `app/api/inventory/route.ts` | GET POST | LariatNative/Sources/LariatDB/InventoryUpdateRepository.swift | LariatNative/Tests/LariatDBTests/InventoryUpdateRepositoryTests.swift | POST: item required else 400; source='toast' + qty>0 + recipe_id + ingredient triggers bom_lines.loss_factor shrinkage (delta = -raw_qty, note explains applied/fallback reason), else free-text or formatted '-qty unit' delta; single INSERT in db.transaction with in-transaction audit_events insert; withIdempotency; location from body; shift_date defaults today; GET filtered by shift_date + location_id; 500 on error. | ✓ |
| `app/api/kds/tickets/[id]/bump/route.js` | POST | LariatNative/Sources/LariatDB/KdsTicketRepository.swift | LariatNative/Tests/LariatDBTests/KdsTicketRepositoryTests.swift | POST: ticket id ≤200 chars else 400; optional JSON body validated via lib/kds validateBumpPayload else 422; 404 if ticket unknown at location; INSERT..ON CONFLICT kept-latest upsert (created_at preserved); in-transaction audit_events insert/correction carrying prior_bumped_at, audit failure rolls back the upsert; withIdempotency (per-bump idempotency key gates re-bump); PUBLIC endpoint by protocol design (no PIN, protocol §3); location from body-or-request; cook_pin stored as hash; 500 on write failure. | ✓ |
| `app/api/kds/tickets/route.js` | GET POST | LariatNative/Sources/LariatDB/KdsTicketRepository.swift | LariatNative/Tests/LariatDBTests/KdsTicketRepositoryTests.swift | POST: order_number required; ≥1 line, each needing item_name + integer quantity≥1 + station (lowercased slug), else 400 with per-line index; placed_at normalized to canonical ISO-8601 else 400; UUIDv7 ids; ticket+lines inserted in one db.transaction with JSONL file audit (lib/auditLog.mjs logAuditAction — NOT audit_events, TEXT ids); withIdempotency; GET is public, location-scoped, returns only un-bumped tickets in the protocol-pinned wire shape, 200 always (empty array = no work); 500 on error. | ✓ |
| `app/api/kitchen-assistant/route.js` | GET POST | LariatNative/Sources/LariatDB/KitchenAssistantEngine.swift | LariatNative/Tests/LariatDBTests/KitchenAssistantEngineTests.swift | POST: message required, ≤2000 chars else 400; deterministic Q-vs-C classifier routes before LLM; PIN-before-LLM hard gate for mutating imperatives (#248) + per-action PIN allowlist defense-in-depth; grounded context redacts manager-only sections for cook tier (#247); server-authoritative calculator + FDA temp/receiving validators (LLM numbers discarded); strict Number.isFinite guards with soft-reject; cross-location beo_add_prep guard; roster validation for gold stars; every write in db.transaction with in-transaction audit_events insert; undo metadata (audit id) returned; JSON action stripped on question path; withIdempotency; location from body-or-request; 502 on LLM failure, 500 on context failure. | ✓ |
| `app/api/kitchen-assistant/undo/route.js` | POST | LariatNative/Sources/LariatDB/AssistantUndoRepository.swift | LariatNative/Tests/LariatDBTests/AssistantUndoRepositoryTests.swift | POST: invalid JSON 400; manager PIN required else 403 (fails closed); undo_audit_id coerced numeric; delegates to undoKitchenAssistantAction which validates the audit event, location scope, and writes a compensating 'correction' returning correctedAuditId; error status passed through from the lib result; withIdempotency; location from body-or-request. | ✓ |
| `app/api/morning/route.js` | GET POST | LariatNative/Sources/LariatModel/Compute/MorningCompute.swift (+ LariatNative/Sources/LariatDB/MorningRepository.swift) | LariatNative/Tests/LariatModelTests/MorningComputeTests.swift (+ LariatNative/Tests/LariatDBTests/MorningRepositoryTests.swift) | GET: ?date must match YYYY-MM-DD else todayISO; location via locationFromRequest; cache-control no-store; 500 on failure. POST: withIdempotency (SW-replay dedupe writes idempotency_keys); 503 if webhook env unset; 502 on Slack non-ok; no audit_events; digest is location-scoped. | ✓ |
| `app/api/performance-reviews/route.ts` | GET POST | LariatNative/Sources/LariatDB/PerformanceReviewsRepository.swift (create/list; score rules in LariatNative/Sources/LariatModel/Compute/PerformanceReviewCompute.swift; JSONL track in LariatNative/Sources/LariatModel/ManagementAuditLogger.swift::logPerformanceReviewLogged) | LariatNative/Tests/LariatDBTests/PerformanceReviewsRepositoryTests.swift (+ LariatNative/Tests/LariatModelTests/PerformanceReviewComputeTests.swift) | requirePin on BOTH GET and POST (HR records); 400 on missing cook_name/review_date/reviewer_name or validateScores failure; two-track audit per docs/PATTERNS.md §3 — audit_events INSERT inside the same db.transaction as the review INSERT, JSONL file audit emitted best-effort AFTER commit (never inside the tx); withIdempotency on POST; location via locationFromBody (POST) / locationFromRequest (GET, location_id-filtered list); 500 generic. | ✓ |
| `app/api/pest/route.ts` | GET POST | LariatNative/Sources/LariatDB/PestRepository.swift (log/load; validation ported in LariatNative/Sources/LariatModel/Compute/PestCompute.swift::validate) | LariatNative/Tests/LariatDBTests/PestRepositoryTests.swift (+ LariatNative/Tests/LariatModelTests/PestComputeTests.swift) | POST: validatePestControl gate returns 400 with reason (entry_type/pest/severity vocab); field clipping (entry_type 64, vendor/technician 100, findings 1000, corrective_action 500, report_path 300); shift_date defaults todayISO; location via locationFromBody; audit_events INSERT in the SAME transaction as the source row (rollback wipes both); withIdempotency; no PIN gate. GET: location-scoped (DEFAULT_LOCATION_ID fallback), newest-first LIMIT 100; 500 generic. | ✓ |
| `app/api/prep-par/route.js` | GET POST DELETE | LariatNative/Sources/LariatDB/PrepParRepository.swift (upsert/delete/load; LariatNative/Sources/LariatModel/Compute/PrepParCompute.swift) | LariatNative/Tests/LariatDBTests/PrepParRepositoryTests.swift (+ LariatNative/Tests/LariatModelTests/PrepParComputeTests.swift) | POST 400 unless recipe_slug or ingredient non-empty; clips (station 64, slug 200, ingredient 200, unit 32, note 500); DELETE 400 on bad id, 404 when row absent in location; audit_events emitted inside the same db.transaction for insert/update/delete; withIdempotency on POST+DELETE; GET location-scoped with optional station_id filter, ordered station/sort_order/recipe/ingredient; 500 generic. | ✓ |
| `app/api/prep-tasks/[id]/route.js` | PATCH DELETE | LariatNative/Sources/LariatDB/PrepRepository.swift (patch/delete — header declares behavior parity with app/api/prep-tasks/[id]/route.js PATCH lifecycle + DELETE) | LariatNative/Tests/LariatDBTests/PrepRepositoryTests.swift | 400 on bad id / bad status / claim+release both / claim without cook / empty patch / blank task; 404 with NO mutation when row absent in (id, location_id) — cross-location IDOR guard; audit_events update carries {before, after} payload, delete carries before payload, both in-transaction (actor_source 'cook_ui'); withIdempotency on both methods; location via locationFromBodyOrRequest; 500 generic. | ✓ |
| `app/api/prep-tasks/route.js` | GET POST | LariatNative/Sources/LariatDB/PrepRepository.swift (create/load — header declares behavior parity with this route's POST create) | LariatNative/Tests/LariatDBTests/PrepRepositoryTests.swift (+ LariatNative/Tests/LariatModelTests/PrepComputeTests.swift) | POST 400 'task required'; clips (task 300, notes 1000, qty 80, recipe_slug 160, station 80); priority clamped to 0-2; status forced 'todo' on insert; actor falls back to assigned_cook_id; audit_events in same transaction; withIdempotency; GET scoped by location_id + shift_date (defaults todayISO) with optional station_id/status filters, done-last then priority-desc ordering; 500 generic. | ✓ |
| `app/api/purchasing/vendor-link/attach/route.js` | POST | LariatNative/Sources/LariatDB/VendorMappingWriteRepository.swift (attachCatalogRow, L128) | LariatNative/Tests/LariatDBTests/VendorMappingWriteRepositoryTests.swift | PIN-gated; body {masterId, catalogKey}; rule rejections via VendorMappingRejectedError with mapped status — 422 no master picked / wrong-vendor row / ingredient mismatch, 404 staple or catalog row not found, 409 staple already has both vendors or item linked elsewhere; single better-sqlite3 transaction emitting 2 in-transaction audit_events; hard-scoped to DEFAULT_LOCATION_ID; no idempotency wrapper; 500 fallback. | ✓ |
| `app/api/purchasing/vendor-link/pair/route.js` | POST | LariatNative/Sources/LariatDB/VendorMappingWriteRepository.swift (pairCatalogRows, L39) | LariatNative/Tests/LariatDBTests/VendorMappingWriteRepositoryTests.swift | PIN-gated; body {syscoKey, shamrockKey, canonicalName}; VendorMappingRejectedError mapping — 422 empty/too-short staple name, wrong-vendor key, catalog row missing/ingredient mismatch; 409 name already linked or item linked to another staple; single transaction emitting 4 in-transaction audit_events (ingredient_masters, 2x ingredient_maps, vendor_prices); DEFAULT_LOCATION_ID scope; no idempotency wrapper; 500 fallback. | ✓ |
| `app/api/receiving/matches/[id]/route.js` | PATCH | LariatNative/Sources/LariatDB/ReceivingRepository.swift (resolveMatch, L326) | LariatNative/Tests/LariatDBTests/ReceivingMatchesTests.swift | PIN-gated + Idempotency-Key dedupe (withIdempotency); 400 bad id / invalid JSON / missing master_id; 404 receiving row (location-scoped via body-or-header location_id) or unknown master; 409 rejected delivery or no positive qty/unit/item; one transaction: receiving_log UPDATE + inventory_updates repoint-or-INSERT + in-transaction audit_events per table (action correction/insert, actor_source manager_ui / receiving_match_resolution) + sync_feed appendOp per table. | ✓ |
| `app/api/receiving/route.js` | POST GET | LariatNative/Sources/LariatDB/ReceivingRepository.swift (record L93, load L33) | LariatNative/Tests/LariatDBTests/ReceivingRepositoryTests.swift | NO PIN gate (cook line UI); POST wrapped in withIdempotency; 400s: missing vendor, unknown category, non-ISO expiration_date, non-finite reading_f/received_qty, corrective_action >500 chars, closed-loop qty/unit shape errors (non-rejected only); 422 + needs_rejection_note (rejected, no note) or needs_corrective_action (accept_with_note, no note) — HACCP rejection outranks input-shape 400; one transaction: receiving_log INSERT + in-transaction audit_events (actor_source cook_ui, note status:category) + sync_feed appendOp + optional inventory_updates credit with its own audit + appendOp; location from body (POST) / header (GET); GET returns per-vendor groups + category summary. | ✓ |
| `app/api/reservations/[id]/route.js` | PATCH DELETE | LariatNative/Sources/LariatDB/ReservationsRepository.swift | LariatNative/Tests/LariatDBTests/ReservationsRepositoryTests.swift | PATCH: bad id → 400; >1 verb → 400 'multiple verbs'; row lookup scoped id+location_id → 404; no effective change → 400 'no change'; party_size int 1..50; clip() length caps; single db.transaction = reservations UPDATE + in-tx audit_events (from_status/to_status/verb) + dining_tables mirror with its own in-tx audit row (stale table_id skipped silently). DELETE: location from query params (default 'default'), 0 changes → 404, in-tx delete audit. Both wrapped in withIdempotency; errors → 500. | ✓ |
| `app/api/reservations/route.js` | GET POST | LariatNative/Sources/LariatDB/ReservationsRepository.swift | LariatNative/Tests/LariatDBTests/ReservationsRepositoryTests.swift | GET: date=YYYY-MM-DD filter (wins over from/to range, upper bound extended '99:99'), status filter, ORDER reservation_at,id, LIMIT 500, location_id-scoped via locationFromRequest. POST: party_name required → 400; party_size integer 1..50 → 400; reservation_at required → 400; source defaults 'manual'; clip() caps; db.transaction INSERT + in-tx postAuditEvent(insert, payload {party_name,party_size,reservation_at}); withIdempotency; locationFromBody; errors → 500. | ✓ |
| `app/api/sanitizer/route.ts` | GET POST | LariatNative/Sources/LariatDB/SanitizerRepository.swift | LariatNative/Tests/LariatDBTests/SanitizerRepositoryTests.swift | POST: validateSanitizerCheck (chemistry/ppm/temp/point_label) → 400; out-of-range reading without corrective_action → 422 with needs_corrective_action + band bounds; classifySanitizer stamps status + required min/max ppm; shift_date defaults todayISO(); single db.transaction = INSERT + postAuditEvent(insert, actor_source=cook_ui, note=breach_reason) + appendOp to sync_feed; withIdempotency; locationFromBody. GET: date param (default today), location-scoped rows + latest-per-point rollup + known points/chemistries; point-in-time model, no PATCH. | ✓ |
| `app/api/sds/route.ts` | GET POST | LariatNative/Sources/LariatDB/SdsRepository.swift | LariatNative/Tests/LariatDBTests/SdsRepositoryTests.swift | POST: validateSds → 400 (product_name required, GHS hazard class, http(s) url, ISO last_reviewed — mirrored by native tests); active defaults 1, last_reviewed defaults todayISO(); clip() caps; db.transaction = INSERT + postAuditEvent(insert, actor_source=cook_ui, payload=row); withIdempotency; locationFromBody; errors → 500. GET: active=1 only, ORDER product_name, location-scoped. | ✓ |
| `app/api/shows/[id]/box-office/[lineId]/route.js` | PATCH | LariatNative/Sources/LariatDB/BoxOfficeRepository.swift | LariatNative/Tests/LariatDBTests/BoxOfficeRepositoryTests.swift | requirePinOrScope('event.box_office') gate; show/line ids must be positive ints → 400; invalid JSON → 400; body.action must equal 'mark_scanned' → 400; missing/already-scanned/location- or show-mismatched line → 404 (idempotent no-op, deliberately NO second audit row so the door scanner can't double-emit); success = UPDATE + in-tx DB audit; withIdempotency; location from body falling back to request. | ✓ |
| `app/api/shows/[id]/box-office/route.js` | GET POST | LariatNative/Sources/LariatDB/BoxOfficeRepository.swift | LariatNative/Tests/LariatDBTests/BoxOfficeRepositoryTests.swift | Both methods gated by requirePinOrScope('event.box_office') (defensive, on top of /api/shows middleware matcher); invalid show id → 400; invalid JSON → 400; repo validation failures (unknown source, non-positive qty) surface as 400; success → 201 with line; withIdempotency on POST; locationFromBody (POST) / locationFromRequest (GET); regulated cash custody → DB audit stream inside the same transaction, NOT JSONL. DICE bulk ingest is out of scope here (scripts/ingest-dice.mjs → bulkUpsertFromDice). | ✓ |
| `app/api/shows/[id]/capacity/route.js` | POST | LariatNative/Sources/LariatDB/ShowsRepository.swift (setCapacityOverride, line 307) | LariatNative/Tests/LariatDBTests/ShowsRepositoryTests.swift (testCapacitySetsOverrideAndWritesOneFileAuditEntry, testCapacityFloorsFractionalOverrides, testCapacityNilClearsTheKey, testCapacityZeroOrNegativeTreatedAsClear, testCapacityPreservesOtherStatusKeys, testCapacityRejectsNonFinite, testCapacityRejectsAboveMax, testCapacityNotFoundForUnknownShow) | requirePinOrScope('event.show_capacity'); withIdempotency; 400 invalid show id/JSON/non-finite/>5000; <=0 treated as clear; fractional floored; 404 show NotFound; single-key surgical merge of shows.status_json inside one db.transaction; file-stream JSONL audit (lib/auditLog.mjs logAuditAction 'show_capacity_set') invoked inside the tx; location-scoped (body location_id \|\| request header) on both SELECT and UPDATE. | ✓ |
| `app/api/shows/[id]/deal/route.js` | GET PUT | LariatNative/Sources/LariatDB/ShowSettlementRepository.swift (upsertDeal, line 54) + LariatNative/Sources/LariatModel/Compute/DealPointsCompute.swift | LariatNative/Tests/LariatDBTests/ShowSettlementRepositoryTests.swift (testInsertNewDealWritesOneAuditRow, testUpdateExistingDealAuditsAsCorrection, testUpsertRollsBackAuditWhenDealInsertFails, testEmptyCookIdFallsBackToUnknown, testUpsertRejectsNegativeGuaranteeBeforeWriting, testUpsertRejectsVsPctAboveOne) + LariatNative/Tests/LariatModelTests/DealPointsComputeTests.swift | hasPinCookie else 401; 400 bad show id/JSON; 422 deal validation (guaranteeCents/buyoutCents non-negative ints, vsPctAfterCosts null or 0-1, costsOffTop array of {label:string, cents:non-neg int}); cookId defaults 'unknown'; withIdempotency on PUT; in-transaction audit_events DB stream via lib/settlementRepo.ts (action 'upsert' first write, 'correction' after); location-scoped via request header; PUT returns full settlement summary. | ✓ |
| `app/api/shows/[id]/sound/[sceneId]/route.js` | PATCH DELETE | LariatNative/Sources/LariatDB/SoundRepository.swift (updateScene line 179, deleteScene line 237) | LariatNative/Tests/LariatDBTests/SoundRepositoryTests.swift (testUpdatePatchesNamePlotAndWritesAudit, testUpdateThrowsNotFoundWhenIdMissing, testUpdateThrowsNotFoundOnLocationMismatch, testUpdateRejectsEmptySceneNameInPatch, testUpdateRejectsEmptyPatch, testUpdatePreservesUnchangedFieldsOnPartialPatch, testDeleteRemovesRowAndWritesAudit, testDeleteThrowsNotFoundOnLocationMismatch) | requirePinOrScope('event.sound_config'); withIdempotency on both verbs; 400 invalid show/scene id, invalid JSON, empty patch, scene_name errors; 404 NotFound (incl. location mismatch); notes truncated to 4000; file-audit row (lib/auditLog.mjs stream) written inside the same db.transaction as the UPDATE/DELETE so audit failure rolls back the mutation; location-scoped (body \|\| request). | ✓ |
| `app/api/shows/[id]/sound/route.js` | GET POST | LariatNative/Sources/LariatDB/SoundRepository.swift (createScene line 107, listScenes line 30) | LariatNative/Tests/LariatDBTests/SoundRepositoryTests.swift (testCreateWritesRowPlusAuditEntry, testCreateRejectsEmptySceneName, testCreateRejectsNonPositiveShowId, testCreateSceneRollsBackWhenAuditFails, testListNewestFirstWithPlotRoundTrip, testListRespectsLocationScoping) | requirePinOrScope('event.sound_config') on GET and POST; withIdempotency on POST; 400 invalid show id/JSON/missing scene_name (trimmed); plot defaults {channels:[],monitors:[]}; notes truncated 4000; 201 on create; GET returns scenes + soundCompleteness; file-audit in same db.transaction (lib/soundRepo.ts uses auditLog.mjs operational stream, not the regulated DB stream); POST location from body only, GET from request. | ✓ |
| `app/api/shows/[id]/sound/spl/route.js` | GET POST | LariatNative/Sources/LariatDB/SoundRepository.swift (appendSplReading line 291, listSplReadings line 46) + LariatNative/Sources/LariatModel/Compute/SplTelemetryCompute.swift (summarizeSpl) | LariatNative/Tests/LariatDBTests/SoundRepositoryTests.swift (testAppendInsertsRowPlusSingleAuditEntry, testAppendRejectsDbValueOutOf30To160, testAppendRejectsNonPositiveShowId, testAppendPersistsPositiveSceneIdAndCoercesInvalidToNil, testListReadingsOldestToNewestBoundedByLimit, testListReadingsRespectsLocationScoping) + LariatNative/Tests/LariatModelTests/SplTelemetryComputeTests.swift | requirePinOrScope('event.sound_config'); withIdempotency on POST; 400 invalid show id/JSON, db_value missing/non-finite (repo layer additionally enforces 30-160 dB range), scene_id must be positive int if given; notes truncated 2000; 201 returns inserted row + last-200 readings + summarizeSpl vs latest scene limit; file-audit via auditLog.mjs stream; POST location from body, GET from request; GET supports ?since=/?limit=. | ✓ |
| `app/api/shows/[id]/stage/route.js` | GET POST | LariatNative/Sources/LariatDB/StageRepository.swift (upsertSetup line 74, getSetup line 30) | LariatNative/Tests/LariatDBTests/StageRepositoryTests.swift (testUpsertCreatesThenUpdatesSingleRow, testUpsertRejectsUnknownRoomConfigBeforeWriting, testUpsertRejectsNonPositiveShowId, testUpsertWritesCreatedThenUpdatedAuditEntries, testUpsertScopedByLocation, testRunOfShowAndRidersRoundTrip, testMalformedRiderJsonDegradesToEmptyObject, testUpsertRollsBackWhenAuditFails) | requirePinOrScope('event.stage_setup'); withIdempotency on POST; 400 invalid show id/JSON or room_config not in KNOWN_ROOM_CONFIGS (rejected before writing); notes truncated 4000; UPSERT semantics — 201 when created, 200 when updated; file-audit row (auditLog.mjs operational stream) in same tx as the upsert; POST location from body, GET from request; GET returns setup + stageCompleteness + KNOWN_ROOM_CONFIGS. | ✓ |
| `app/api/sick-leave/route.js` | GET POST | LariatNative/Sources/LariatDB/SickLeaveRepository.swift (accrue/use; rule math in LariatNative/Sources/LariatModel/Compute/SickLeaveCompute.swift) | LariatNative/Tests/LariatDBTests/SickLeaveRepositoryTests.swift (plus LariatModelTests/SickLeaveComputeTests.swift) | PIN gate pic.sick_leave via hasPinOrTempPin (403); 400 on kind not in {accrual,use}, missing cook_id, year outside 2000-2100, non-positive hours, dated_on not YYYY-MM-DD; 422 cap-reached / insufficient balance; upsert-shell -> pure rule -> UPDATE + in-transaction audit_events (postAuditEvent inside db.transaction); idempotent via withIdempotency; location from body (POST) / header-or-default (GET). | ✓ |
| `app/api/sick-worker/route.js` | GET POST PATCH | LariatNative/Sources/LariatDB/SickWorkerRepository.swift (file/clear; LariatNative/Sources/LariatModel/Compute/SickWorkerCompute.swift) | LariatNative/Tests/LariatDBTests/SickWorkerRepositoryTests.swift (plus LariatModelTests/SickWorkerComputeTests.swift) | PIN gate pic.sick_worker on POST/PATCH (403); POST 400 via validateSickReport, PII kept out of audit note (full row in payload JSON); PATCH 400 missing id/clearance_source, 404 unknown report, 409 already-cleared with the pre-check + UPDATE in ONE transaction to block double-clearance; in-transaction audit_events on both writes; idempotent via withIdempotency; location body (writes) / header (GET); GET history (?all=1) is full-PIN-cookie-only, active list open. | ✓ |
| `app/api/signoff/route.ts` | POST | LariatNative/Sources/LariatDB/LineCheckRepository.swift (signoff — L5 staff_flags minor gate, L6 SickWorkerCompute.cookHasActiveExclusion, unnoted-fails check, INSERT + audit in one transaction) | LariatNative/Tests/LariatDBTests/LineCheckRepositoryTests.swift (explicit 'L5 + L6 signoff-gate suite (parity: tests/js/test-signoff-gates-api.mjs)' plus testSignoffBlocksUnnotedFails / testPostEntryAndSignoffWithAudit) | No PIN (cook_ui actor); 400 missing shift_date/station_id/cook_id (clipped); gate order regulatory>operational: 422 L5 minor (active staff_flags row, citation returned), 422 L6 reportable-illness exclusion (citation returned), 409 latest fail entries lacking corrective-action note; INSERT + in-transaction audit_events; idempotent via withIdempotency; location from body. | ✓ |
| `app/api/specials/saved/[id]/export/route.js` | POST | LariatNative/Sources/LariatDB/SpecialsRepository.swift (export; CSV/procedure rules in LariatNative/Sources/LariatModel/Compute/SpecialsExport.swift) | LariatNative/Tests/LariatDBTests/SpecialsRepositoryTests.swift (plus LariatModelTests/SpecialsExportComputeTests.swift) | Full-PIN-cookie only (401; temp PIN NOT accepted, unlike sibling routes); 400 slug/yield_qty/yield_unit/category>64/procedure_override type; 404 not found (location-scoped); 410 archived; 409 slug collision vs entities_recipes, tolerating missing table on fresh DBs; last_exported_at UPDATE + JSONL logAuditAction in transaction (no audit_events row); NOT wrapped in withIdempotency — every export re-stamps last_exported_at. | ✓ |
| `app/api/specials/saved/[id]/promote/route.js` | POST | LariatNative/Sources/LariatDB/SpecialsRepository.swift (promote; LariatNative/Sources/LariatModel/Compute/SpecialsPromotionCompute.swift) | LariatNative/Tests/LariatDBTests/SpecialsRepositoryTests.swift (plus LariatModelTests/SpecialsPromotionComputeTests.swift) | PIN menu.specials_edit via hasPinOrTempPin (401, no-store headers); body optional — 400 invalid JSON / bad menu_item_name / servings not positive finite; 404 not_found, 410 archived, 400 no costed ingredients; transactional audit_events written inside promoteSpecialToMenu plus operator-facing JSONL trail after; idempotent both via withIdempotency and repromote-refreshes-same-record semantics; location-scoped (cross-location 404). | ✓ |
| `app/api/specials/saved/[id]/route.js` | GET PATCH DELETE | LariatNative/Sources/LariatDB/SpecialsRepository.swift (get/update/archive; validators in LariatNative/Sources/LariatModel/Compute/SpecialsValidators.swift) | LariatNative/Tests/LariatDBTests/SpecialsRepositoryTests.swift (plus LariatModelTests/SpecialsValidatorsTests.swift) | PIN menu.specials_edit via hasPinOrTempPin (401) on all three methods; PATCH 400 invalid JSON / non-editable fields (validatePatchKeys, rejected list returned) / bad name / non-string scratch_notes (clipped to SCRATCH_NOTES_MAX); 404 location-scoped not-found; DELETE idempotent (already-archived returns 200 ok); JSONL logAuditAction inside db.transaction (no audit_events); withIdempotency on PATCH/DELETE. | ✓ |
| `app/api/specials/saved/route.js` | GET POST | LariatNative/Sources/LariatDB/SpecialsRepository.swift (create/list; JSONL trail in LariatNative/Sources/LariatModel/SpecialsAuditLog.swift) | LariatNative/Tests/LariatDBTests/SpecialsRepositoryTests.swift | PIN menu.specials_edit via hasPinOrTempPin (401) on both methods; POST 400 invalid JSON / non-object body / bad name / all-empty session content / invalid cost_breakdown or sources JSON; pantry/prompt/scratch clipped, ai_answer intentionally uncapped; JSONL logAuditAction in transaction (no audit_events); idempotent via withIdempotency; GET lists location-scoped, archived_at IS NULL, newest-first with 120-char snippet. | ✓ |
| `app/api/temp-log/route.js` | GET POST | LariatNative/Sources/LariatDB/TempLogRepository.swift (postReading via AuditedWriteRunner; rules in LariatNative/Sources/LariatModel/Compute/TempLogCompute.swift) | LariatNative/Tests/LariatDBTests/TempLogRepositoryTests.swift (plus LariatNative/Tests/LariatModelTests/TempLogComputeTests.swift) | 400 missing shift_date/point_id/reading_f, unknown point, bad input, or corrective_action >500 chars; 422 out-of-range reading without corrective note (needs_corrective_action); 403 back-dating past today without manager PIN or temp-PIN scope 'haccp.back_date' (gate off when LARIAT_PIN unset); withIdempotency; INSERT + audit_events (postAuditEvent, actor_source cook_ui, out_of_range/calibration_warning notes) + sync_feed appendOp all in ONE db.transaction; advisory (never-blocking) calibration_warning when probe_id given; location_id from body on POST, from query on GET; GET filters shift_date+location_id and adds classifyReadings summary (opt-out ?summary=0). | ✓ |
| `app/api/thermometer-calibrations/route.js` | GET POST | LariatNative/Sources/LariatDB/CalibrationRepository.swift (post via AuditedWriteRunner; thresholds/altitude/next-due in LariatNative/Sources/LariatModel/Compute/CalibrationCompute.swift + ProbeCompute.swift) | LariatNative/Tests/LariatDBTests/CalibrationRepositoryTests.swift | 400 for missing thermometer_id, unknown method, non-numeric reading_f/elevation_ft, note >500 chars, or non-positive-integer frequency_days; no PIN gate; withIdempotency; persists pass AND fail (fail row IS the audit trail); INSERT + audit_events in one db.transaction with note 'fail:<probe>:<method>' on failures; location from body (POST) / query (GET); GET returns classifyProbes summary (ok/due_soon/overdue/failed/unknown) with optional ?probe_id / ?entries=1. | ✓ |
| `app/api/tip-pool/route.js` | GET POST | LariatNative/Sources/LariatDB/TipPoolRepository.swift (add via AuditedWriteRunner, eligibility gate thrown pre-INSERT; rules in LariatNative/Sources/LariatModel/Compute/TipPoolCompute.swift) | LariatNative/Tests/LariatDBTests/TipPoolRepositoryTests.swift (plus LariatNative/Tests/LariatModelTests/TipPoolComputeTests.swift) | 403 without manager PIN or temp-PIN scope 'pic.tip_pool' when pinRequiredForPic(); 422 non-integer amount_cents (integer cents end-to-end); 400 validateDistributionShape failure; 422 with COMPS §3.4 citation when a manager/owner (staff_flags, effective_to IS NULL) receives kind=tip_pool; withIdempotency; INSERT + audit_events (actor_source pic_ui) in one db.transaction; location from body (POST) / query (GET); GET date-range summary via summarizePool plus 2026 CO comps constants. | ✓ |
| `app/api/tphc/route.js` | GET POST PATCH | LariatNative/Sources/LariatDB/TphcRepository.swift (start + discard via AuditedWriteRunner; rules in LariatNative/Sources/LariatModel/Compute/TphcCompute.swift) | LariatNative/Tests/LariatDBTests/TphcRepositoryTests.swift (plus LariatNative/Tests/LariatModelTests/TphcComputeTests.swift) | POST: 400 on validateTphcCreate failure, cutoff_at computed server-side from started_at+kind; PATCH: 400 bad id or discard_reason not in TPHC_DISCARD_REASONS, 404 unknown id AND 404 (not 403) cross-location IDOR guard, 409 already-discarded — existence+guards+UPDATE+audit all in ONE db.transaction (prevents double-stamp race); both writes withIdempotency and in-tx audit_events (cook_ui); no PIN gate; GET location-scoped active batches + scanActiveTphc(ok/warning/expired). | ✓ |
| `app/api/wage-notices/route.js` | GET POST | LariatNative/Sources/LariatDB/WageNoticeRepository.swift (sign via AuditedWriteRunner; shape/freshness rules in LariatNative/Sources/LariatModel/Compute/WageNoticeCompute.swift) | LariatNative/Tests/LariatDBTests/WageNoticeRepositoryTests.swift (plus LariatNative/Tests/LariatModelTests/WageNoticeComputeTests.swift) | 403 without manager PIN or temp-PIN scope 'pic.wage_notices' when pinRequiredForPic(); 400 missing cook_id or validateNoticeShape failure (integer-cents wage_rate_cents/tip_credit_cents); withIdempotency; INSERT + audit_events (actor_source pic_ui, note '<reason>:<pay_basis>', no shift_date) in one db.transaction; reads open at location level: GET returns latest-per-cook + summarizeFreshness, or full per-cook history with requiresNewNotice refresh demo when ?cook_id given; location from body (POST) / query (GET). | ✓ |

## edge-retained (21)

| Route | Methods | Why it stays on the edge |
|---|---|---|
| `app/api/auth/pin/route.ts` | GET POST DELETE | This is the master-PIN cookie-session machinery that gates every retained web surface (PWA/remote, guest BEO share management) — edge auth machinery per the edge-blockers scope. No persistent state is mutated (cookie + in-memory limiter only); the native app has its own gate (PinVerifier/PinSessionStore) but the HTTP cookie flow must stay on the edge. |
| `app/api/auth/temp-pin/login/route.js` | POST | This is the cookie-issuing session exchange for web surfaces — edge auth machinery in the same family as /api/auth/pin, and the cookie it mints only means anything to the web edge (lib/pin.ts::hasPinOrTempPin). The verification RULES are natively owned (LariatModel/TempPinVerifier.swift + Tests/LariatModelTests/TempPinVerifierTests.swift, incl. scope grant and revoked-fails-immediately via TempPinRepositoryTests), but the HTTP login/cookie flow itself stays on the edge; the only write is the login audit row, which travels with the transport. |
| `app/api/beo/[id]/share-token/route.js` | POST | Part of the guest BEO share-and-sign machinery the edge-blockers doc reserves for the edge server; the native model explicitly declines ownership — LariatNative/Sources/LariatModel/BeoRecords.swift:44-45 documents share_token lifecycle columns as 'edge-blocker territory — never selected or written natively'. |
| `app/api/beo/share/[token]/route.js` | GET | This is the tokenized public share read the edge-blockers doc's 'Guest BEO share-and-sign' entry explicitly says the edge server must keep — an external guest opens this URL in their own browser, which a macOS/iPad app cannot serve. |
| `app/api/beo/share/[token]/sign/route.js` | POST | The edge-blockers doc's seed entry names exactly this: 'the sign-and-confirm POST + its audited write' with actor_source beo_client_share stays on the edge because the signer is an external guest on a public URL. |
| `app/api/cleaning-schedule/route.js` | GET POST PATCH DELETE | UNPORTED WRITE GAP filed here only because no ledger bucket fits: live caller app/admin/cleaning-schedule/CleaningScheduleEditor.jsx (not dead), native only READS cleaning_schedule (CommandRepository.swift:160 command-summary aggregate — no native CRUD, so not ported), and the route does NOT appear in lariat-native-edge-blockers.md (so edge retention is de facto, not ratified). Needs either a native schedule-admin port (C5 wave 2 'cleaning') or an explicit edge-blocker entry. |
| `app/api/cloud-bridge/dead-letters/[id]/drop/route.js` | POST | Explicitly named in lariat-native-edge-blockers.md ratified A5.4 option-B entry (2026-07-03, user-ratified): dead-letter triage writes (requeue/drop) are transport machinery that stays on the edge server; re-implementing in Swift was declined as high-risk for zero operator gain. |
| `app/api/cloud-bridge/dead-letters/[id]/requeue/route.js` | POST | Same ratified A5.4 option-B edge-blockers entry names this route: the dead-letter queue's requeue/drop writes are HTTP-replay transport machinery kept on the edge alongside /management/cloud-bridge triage UI. |
| `app/api/cloud-bridge/dead-letters/route.js` | GET | Read-only, but explicitly named in the ratified A5.4 option-B edge-blockers entry — it feeds the /management/cloud-bridge triage UI (inspect payload) that stays on the edge. The native CloudBridgeStatusRepository covers only depth counts (testDeadLetterTotalIsNotLocationScoped), not the payload listing this route serves. |
| `app/api/cloud-bridge/status/route.js` | GET | Named in the ratified A5.4 option-B edge-blockers entry as part of the edge transport surface; the READ-ONLY native counterpart that shipped instead is the manager.cloudBridge status board (CloudBridgeStatusView + CloudBridgeStatusRepository, cited as informational reader — natives do not call this route). |
| `app/api/compute/status/route.js` | GET POST | The snapshot-writing compute engine has NO native owner (no INSERT INTO margin_snapshots/accounting_variance anywhere in LariatNative/Sources; A4 spec gap-audit ports only the read-side computeCostVariance card) and native CostingRepository READS accounting_variance, so the web engine remains the sole producer — it cannot be dead (also auto-triggered by app/api/receiving/route.js and covered by tests/js/test-compute-status-route.mjs). It is NOT listed in lariat-native-edge-blockers.md, so this is a genuine C1 gap: engine + trigger stay on the edge today but lack a ratified blocker entry or a native port plan — needs human review. |
| `app/api/discover/route.js` | GET | Explicitly listed in docs/superpowers/specs/lariat-native-edge-blockers.md under the RATIFIED peers/cloud-bridge transport entry (2026-07-03, option B): 'app/api/discover/route.js' is part of the mDNS/HTTP discovery transport that stays on the edge because the native app has no server surface for peers to probe. |
| `app/api/locations/route.js` | GET POST | NOT ported: no INSERT/UPDATE against the locations table exists anywhere in LariatNative/Sources. Not dead: live callers at app/setup/page.jsx (first-run venue naming POST) and app/_components/Sidebar.jsx (GET), plus tests/js/test-setup-flow.mjs. It is also NOT listed in docs/superpowers/specs/lariat-native-edge-blockers.md — classified edge-retained as web first-run-setup machinery, but this is a judgment call: it needs a human decision to either port venue seed/rename to native or ratify it in the blocker log. |
| `app/api/peers/route.js` | GET | app/api/peers/route is named verbatim in the RATIFIED A5.4 option B entry of docs/superpowers/specs/lariat-native-edge-blockers.md — mDNS discovery and peer transport stay on the web edge; loadPeersAndHub (lib/peers.ts -> mdnsDiscovery + hubElection) performs pure network reads, no DB mutation. |
| `app/api/peers/sync-since/route.js` | GET | Named verbatim in the RATIFIED A5.4 option B edge-blockers entry — peer keypair/trust crypto and sync-since replay are transport machinery that stays on the edge (native got only the read-only CloudBridgeStatusView). The peer_trust.last_seen_at UPDATE (lib/peerTrust.ts::touchPeerLastSeen) is a transport-owned write. |
| `app/api/performance-reviews/[id]/route.ts` | DELETE | PORT GAP flagged for human review, not a ratified edge blocker: native PerformanceReviewsRepository.swift has only list/create — no delete — and no LariatNative test touches performance_reviews deletion; the route has a live caller (app/management/performance-reviews/PerformanceReviewBoard.tsx:126) so it is not dead, and the edge-blockers spec does not cover it. It de facto stays on the web edge until a native delete (ideally adding the missing audit) lands. |
| `app/api/preshift-notes/route.ts` | GET POST | PORT GAP flagged for human review, not a ratified edge blocker: no native writer for preshift_notes exists anywhere in LariatNative — the only native touch is a read-only COUNT for the command rollup (LariatNative/Sources/LariatDB/CommandRepository.swift:167); the route has a live caller (app/_components/PreshiftNotes.jsx:53 on the Today page) so it is not dead, and it is absent from the edge-blockers spec. The NULL-tolerant upsert rules stay web-owned until a native port lands. |
| `app/api/recipes/[slug]/photos/[id]/route.js` | DELETE PATCH | FORCED-CHOICE PLACEHOLDER — this is a live, unported write path that fits none of the ledger categories cleanly: no native owner (zero photo hits in LariatNative), NOT listed in lariat-native-edge-blockers.md, and not dead (live callers RecipePhotoStrip.jsx / RecipePhotoUploader.jsx under app/recipes/[slug]/ plus tests/js/test-recipe-photos-api.mjs). Needs a human decision: port to native (with the on-disk photo store) or ratify a new edge-blocker entry. |
| `app/api/recipes/[slug]/photos/route.js` | GET POST | FORCED-CHOICE PLACEHOLDER — same C1 gap as the sibling item route: live callers in app/recipes/[slug]/edit (RecipePhotoUploader) and no native owner anywhere in LariatNative, but also absent from the edge-blockers spec, so 'edge-retained' is not a ratified decision. The file-on-disk store + multipart handling make it a distinct porting problem; flag for human review. |
| `app/api/recipes/[slug]/route.js` | GET PUT | GAP ROUTE: live write (called by app/recipes/[slug]/edit/RecipeEditForm.jsx) with NO native writer — LariatNative only reads recipes.json (DishBridgeRecipeLoader) and checks entities_recipes slug collisions (SpecialsRepository); it is also absent from lariat-native-edge-blockers.md. Classified edge-retained as the least-destructive bucket, but it is really an unported write needing a port decision or a blocker-log entry. |
| `app/api/service-hours/route.js` | GET POST PATCH DELETE | GAP ROUTE: live write (called by app/admin/service-hours/ServiceHoursEditor.jsx) with zero native trace — grep for service_hours/ServiceHours/opens_at across LariatNative Sources+Tests returns nothing — and it is not in lariat-native-edge-blockers.md. Classified edge-retained as the least-destructive bucket; really an unported write needing a port decision or a blocker-log entry (plus a missing-audit follow-up). |

## read-only (37)

| Route | Methods | Native reader | Rules |
|---|---|---|---|
| `app/api/analytics/operators/route.js` | GET | — | PIN-gated by middleware via /api/analytics prefix (no in-route requirePin); window param must be 7\|30\|90 else 400, malformed ?date falls back to today; locationFromRequest scoping; no-store; generic 500. |
| `app/api/audit/log/route.js` | GET | LariatNative/Sources/LariatDB/ManagementAuditLogReader.swift | In-route requirePin via hasPinCookie/pinRequiredForPic (401, supports HMAC-signed cookie); reads the management-actions JSONL via lib/auditLog.mjs with ?action \| ?slug \| ?limit (default 100, applied twice); no location scoping (JSONL is global); generic 500 that never echoes internals. |
| `app/api/auth/temp-pin/list/route.js` | GET | LariatNative/Sources/LariatDB/TempPinRepository.swift | requirePin; metadata only (id/label/scopes/issued_at/expires_at — never pin_hash or raw PIN, spec invariant 4); active filter revoked_at IS NULL AND datetime(expires_at) > datetime('now'); location_id filter via locationFromRequest; generic 500. |
| `app/api/beo/cascade/route.js` | GET | LariatNative/Sources/LariatDB/BeoCascadeRepository.swift (cascade(eventId:locationId:), engine via LariatModel/BeoCascadeClient.swift shelling to the same scripts/beo_cascade_cli.py) | No PIN; 400 missing/non-positive event_id; 404 with identical message for missing vs wrong-location event (no cross-location leak; location via ?location=, default 'default'); CascadeError from the engine returned as HTTP 200 with error field + empty arrays (banner semantics); qtyInYieldUnits=true; 500 catch-all; no writes, no audit. |
| `app/api/beo/fire-schedule/route.js` | GET | LariatNative/Sources/LariatDB/BeoFireScheduleRepository.swift (schedule date-scope and event-scope overloads) + LariatModel/Compute/BeoFireScheduleCompute.swift (resolveSchedule port) | PUBLIC — no PIN (wall-iPad per-station rollup, spec T7); event_id scope when a positive integer param is present, else date scope defaulting to todayISO(); location param default 'default' filters beo_courses.location_id joined to beo_events; only course-bound line items loaded; 500 catch-all; no writes, no audit. |
| `app/api/beo/prep-history/route.js` | GET | LariatNative/Sources/LariatDB/BeoPrepHistoryRepository.swift (itemPrepHistory, recentEvents, prepMedians, recipePrepHistory) + LariatModel/Compute/BeoPrepHistoryCompute.swift | requirePinOrScope('menu.prep_history') (401); item query params capped at 50 per request; optional limit param; recent=1 adds a recent-events list; locationFromRequest scoping on all queries; 500 catch-all; no writes, no audit. |
| `app/api/command/alerts/route.js` | GET | LariatNative/Sources/LariatModel/Compute/CommandCompute.swift | GET only, no write path, no auth gate; ?date= validated against YYYY-MM-DD regex else falls back to todayISO; location via locationFromRequest; computes lib/commandCenter summarize()+alertsFor() and returns red/amber counts + alerts array; 500 on failure. |
| `app/api/command/summary/route.js` | GET | LariatNative/Sources/LariatModel/Compute/CommandCompute.swift | GET only, no write path, no auth gate; ?date= validated against YYYY-MM-DD regex else todayISO; location via locationFromRequest; returns lib/commandCenter summarize() aggregate (sales/inventory/labor/food-safety/reservations/prep/tables/waste/moves); 500 on failure. |
| `app/api/corrective-actions/route.js` | GET | LariatNative/Sources/LariatModel/Compute/HaccpPlanCompute.swift | No PIN (documented informational read for cook on shift); no idempotency wrapper (GET); ?date validated against ISO regex else today; location-scoped via locationFromRequest; station_id filter narrows line_check rows AND drops the temp_log union; merges temp_log corrective_action rows + failed line_check_entries notes via mergeCorrectiveActions; 500 catch-all. |
| `app/api/costing/depletion-exceptions/route.js` | GET | LariatNative/Sources/LariatDB/DepletionExceptionsRepository.swift | PIN-gated twice (middleware /api/costing matcher + in-route requirePin); location-scoped via locationFromRequest; optional ?period label filter; ?limit clamped 1-1000 (default 200); pure read via listDepletionExceptions; 500 catch-all. |
| `app/api/costing/route.js` | GET | LariatNative/Sources/LariatModel/Compute/CostVarianceCompute.swift | PIN-gated (in-route requirePin + /api/costing middleware matcher); location-scoped via locationFromRequest; pure computation in lib/costingBenchmarks.mjs (computeCostVariance + readLastCostingIngest); response caps rows to top-5 to bound payload; D6 fields additive-only; 500 catch-all. |
| `app/api/costing/variance-attribution/route.js` | GET | LariatNative/Sources/LariatDB/VarianceAttributionRepository.swift | PIN-gated by middleware /api/costing/* prefix ONLY (no in-route requirePin — relies on the matcher); ?from/?to must match YYYY-MM-DD regex else 400; default = two most recent periods; location-scoped; Cache-Control: no-store on all responses; 500 catch-all. |
| `app/api/datapack/search/route.js` | GET | LariatNative/Sources/LariatModel/Compute/DatapackSearchCompute.swift | No PIN, no location scoping (shared reference datapack); 503 when datapack not installed; op multiplexer {search(default lexical FTS)\|stats\|usda_food\|off_product\|fda_section\|wikibooks_page\|semantic\|hybrid}; 400 missing/invalid q, source not in {usda,off,wikibooks,fda,all}, bucket not in {recipes,techniques,safety,ingredients}, bad FTS syntax; 404 entity not found; 502 semantic/hybrid model-load failure; query clipped to 240 chars, limit clamped 1-100 (default 20); raw=1 passes FTS5 expression through, default wraps as quoted phrase. |
| `app/api/dish-coverage/route.ts` | GET | LariatNative/Sources/LariatModel/Compute/DishCostBridge.swift (computeDishCoverage L504, DishCoverageReport L136 — mirrors lib/dishCostBridge.ts L407-460) | GET only; delegates entirely to computeDishCoverage(location_id) from lib/dishCostBridge; 500 on compute failure; location-scoped via locationFromRequest with DEFAULT_LOCATION_ID fallback; no writes, no audit, no idempotency needed. |
| `app/api/food-safety/haccp-plan/route.js` | GET | LariatNative/Sources/LariatDB/HaccpPlanRepository.swift + LariatNative/Sources/LariatModel/Compute/HaccpPlanCompute.swift | GET only; date param validated against ^\d{4}-\d{2}-\d{2}$ else falls back to todayISO(); location via locationFromRequest; intentionally NOT PIN-gated (inspector-facing document); cache-control no-store; 500 on build failure; pure aggregation via buildHaccpPlan, no mutation. |
| `app/api/health/route.ts` | GET | — | GET only, intentionally NOT PIN-gated; runs timeout-bounded (2.5s) probes (sqlite count, recipes.json cache, compliance.db, datapack symlink, ollama /api/tags, PIN-gate configured, mDNS status, Toast/7shifts/Prism env creds); test releases neutralize optional-integration failures; required probes (sqlite/cache/pin_gate) failing → status 'down' + HTTP 503, optional failures → 'degraded'; no-store; no mutation. |
| `app/api/lari/predictions/route.js` | GET | LariatNative/Sources/LariatDB/LariPredictionsRepository.swift | GET only; requirePin() gate (defense-in-depth over middleware); surface ∈ {beo,sound,host} else 200 with empty predictions + note (no 4xx); surface=sound requires integer show_id>0 else 400, unknown show returns empty predictions + note; location via ?location defaulting DEFAULT_LOCATION_ID; deterministic prediction builders over waitlist/show/BEO tables; no writes anywhere; 500 on error. |
| `app/api/menu-engineering/margin-deltas/route.js` | GET | LariatNative/Sources/LariatDB/MarginDeltasRepository.swift | GET only; requirePin() in-route re-check on top of the /api/menu-engineering/:path* middleware matcher (curl/replay cannot bypass); params clamped — days [1,90] default 7, minPct [0,1000] default 5, limit [1,500] default 50, absent/NaN → default not min; location via locationFromRequest; math delegated to lib/marginDeltas.ts listMarginDeltas; no writes; 500 on error. |
| `app/api/menu-engineering/route.js` | GET | LariatNative/Sources/LariatDB/MenuEngineeringRepository.swift | GET only; requirePin(); location via ?location defaulting DEFAULT_LOCATION_ID; delegates entirely to computeMenuEngineering(loc) (lib/menuEngineering); no writes; 500 with error message string on failure. |
| `app/api/purchasing/vendor-catalog/route.js` | GET | LariatNative/Sources/LariatDB/VendorMappingRepository.swift (searchVendorCatalog, summarizeMappingCoverage) | PIN-gated (requirePin); 422 unless vendor is sysco\|shamrock; optional q/unlinkedOnly/limit params; hard-scoped to DEFAULT_LOCATION_ID (ignores request location header); no audit; 500 catch-all. |
| `app/api/purchasing/vendor-compare/route.js` | GET | LariatNative/Sources/LariatDB/VendorCompareRepository.swift (listVendorCompareRows) + VendorMappingRepository.swift (listSingleVendorMasters, summarizeMappingCoverage) | PIN-gated; limit clamped to 1..1000 (default 200); hard-scoped to DEFAULT_LOCATION_ID; no audit; 500 catch-all. |
| `app/api/receiving/matches/route.js` | GET | LariatNative/Sources/LariatDB/ReceivingRepository.swift (loadUnmatched, L281) | PIN-gated; location_id from request header; SELECT-only manager queue — accepted/accepted_with_note rows with positive qty + non-blank unit and match_status unmatched\|ambiguous, ordered created_at DESC; no audit; 500 catch-all. |
| `app/api/recipes/[slug]/photos/[id]/raw/route.js` | GET | — | No PIN gate (staff-visible recipe pages); scoped by id + recipe_slug + location_id (header) + deleted_at IS NULL; 404 no row, 410 DB row present but file missing on disk; streams bytes with Content-Type/Length and Cache-Control private,max-age=300. |
| `app/api/recipes/route.js` | GET | LariatNative/Sources/LariatModel/DishBridgeRecipeLoader.swift | GET only: q (name/ingredient substring) and allergen filters over lib/data getRecipes() (data/cache/recipes.json); always 200 with slimmed list (slug, name, counts, allergens, sub_recipes); no auth, no location scoping (file cache is location-agnostic), no audit. |
| `app/api/setup/status/route.js` | GET | — | GET only: read-only first-run step detection via lib/setupStatus getSetupStatus(loc) (booleans + row counts); intentionally pre-PIN/unauthenticated (excluded from middleware matcher because the wizard must report 'no PIN exists yet'); cache-control: no-store; 500 on error; location via locationFromRequest. |
| `app/api/shows/[id]/settlement/pdf/route.js` | GET | LariatNative/Sources/LariatModel/Compute/SettlementPrintCompute.swift + LariatNative/Sources/LariatDB/ShowSettlementRepository.swift (getSettlement) | hasPinCookie else 401; 400 bad show id; 404 show not found; no mutation — renders getSettlement summary as escaped text/html for browser Save-as-PDF; hardened response headers (strict inline-only CSP, nosniff, no-referrer, cache-control no-store); location-scoped via request header. |
| `app/api/shows/[id]/settlement/route.js` | GET | LariatNative/Sources/LariatDB/ShowSettlementRepository.swift (getSettlement, line 129) | hasPinCookie else 401; 400 bad show id; 404 when getSettlement throws 'not found'; pure settlement read (deal + box-office + toast aggregate), no audit, no mutation; location-scoped via request header. |
| `app/api/shows/route.js` | GET | LariatNative/Sources/LariatDB/ShowsRepository.swift (upcomingShows, pipelineCounts, archiveSearch, archiveEras, getShowById) | PIN gate delegated to middleware.js SENSITIVE_PREFIXES (handler trusts it); dispatch on ?op= — upcoming (weeks default 5, plus 52-week pipelineCounts), playbook (400 invalid show id, 404 not found), archive (?q= substring + ?era= year); 400 on unknown op; no writes, no audit; location-scoped via request header on every op. |
| `app/api/shows/tonight/route.js` | GET | LariatNative/Sources/LariatDB/ShowsRepository.swift (tonightSnapshot line 194) + LariatNative/Sources/LariatModel/Compute/ShowsTonightCompute.swift | requirePin; composed read of shows + previous_show + locations.capacity + stage_setups + latest sound_scenes + box_office_lines summary + computeAttendance with per-show status_json.capacity override beating venue capacity; ?date=YYYY-MM-DD override else server-local today; ?location= param else DEFAULT_LOCATION_ID; explicitly 'Read-only — no mutations here' (route comment); 500 on failure; every sub-query location-scoped. |
| `app/api/specials/route.js` | GET POST | — | GET returns safe Ollama config, ?ping=1 does a 3s /api/tags reachability probe; POST 400 missing/over-2000-char message, PIN-aware grounded-context tier (hasPinCookie gates manager-tier data out of unauthenticated prompts), 500 context build failure, 502 model/timeout errors with operator copy; deterministic computeSandboxCost on extracted cost_special action with payload type-guards; no domain-table mutation — only the shared withIdempotency wrapper touches the idempotency_keys cache; location from body-or-request. |
| `app/api/staff/route.js` | GET | LariatNative/Sources/LariatModel/StaffCatalog.swift (load/isDisplayable/formatDisplayName — doc comment: 'parity with lib/staffDisplay.ts') | No auth, no validation, no audit, no location scoping — returns cleanStaffForPicker(getStaff()): inactive/junk-id/junk-name rows filtered from the cached staff.json for the cook identity picker. |
| `app/api/stations/route.js` | GET | LariatNative/Sources/LariatDB/LineCheckRepository.swift (loadStationList + fetchLatestEntries/hasSignoff/computeProgress; StationProgressCompute in LariatModel) | No auth/audit; ?location= else DEFAULT_LOCATION_ID, today's shift date; latest line_check_entries row per (item,station,date,location) via MAX(id) subquery (documented fix for GROUP BY undefined behavior); per-station progress {total,done,flagged,signedOff} joined against station_signoffs; SELECT-only. |
| `app/api/temp-log/points/route.js` | GET | LariatNative/Sources/LariatModel/Compute/TempLogCompute.swift (static `points` registry, line 85, + getTempPoint) | GET returns the static TempPoints registry from lib/tempLog; no DB, no PIN, no location scoping; 500 on unexpected error only. |
| `app/api/transcribe/route.ts` | GET POST | — | GET = capability probe {enabled}; POST: 503 when LARIAT_WHISPER!=1 or pipeline load/inference fails, 400 empty body or invalid WAV (strict 16kHz mono PCM16/float32), 413 body >2MB (~60s cap); no PIN, no DB, no location scoping, no audit — transcript returned to caller only. |
| `app/api/unmapped/route.js` | GET | — | GET only; location via locationFromRequest; delegates entirely to lib/costingBenchmarks.mjs computeUnmapped (reason-priority ordering, rows capped at 50, unmapped_pct); no PIN; 500 on error. |
| `app/api/vendor-prices/history/route.js` | GET | LariatNative/Sources/LariatModel/Compute/PriceShockCompute.swift (PriceSeriesCompute, line 120 — pure port of lib/vendorPricesRepo.ts listPriceSeries delta rule) | 400 missing vendor or sku; limit default 100, clamped to <=1000 here and re-clamped in lib/vendorPricesRepo.listPriceSeries (which owns the SQL); empty history is a truthful 200 with series:[]; location via query (locationFromRequest); no PIN; 500 on error. |
| `app/api/vendor-prices/shocks/route.js` | GET | LariatNative/Sources/LariatDB/PriceShockRepository.swift + LariatNative/Sources/LariatModel/Compute/PriceShockCompute.swift | All params optional with silent clamps: days default 7 [1,90], minPct default 5 [0,1000], limit default 50 [1,500]; math/clamping owned by lib/vendorPricesRepo.listPriceShocks; case-insensitive exact-match category post-filter (applied AFTER limit); location via query; no PIN; 500 on error. |

## dead (1)

| Route | Evidence it is unreachable/frozen |
|---|---|
| `app/api/analytics/route.js` | Zero callers: grep for /api/analytics across app/**, lib/**, components/**, tests/**, desktop/**, app/v2/** finds nothing outside app/api (unlike /api/audit/log which has page + RoleProvider callers). Superseded by the server-rendered app/analytics/page.jsx, which reads toast_sales_* tables directly from the DB (and is what the native AnalyticsRepository.swift mirrors); this route aggregates the older sales_lines/spend_monthly dataset and has no test either. |

## Next actions (C1 → C5)

1. ~~Finish the verify pass on the 41 unverified ported-write routes~~ **DONE 2026-07-07** (feat/lariat-native-c1-verify-41 — all 71 now `✓`; refutations fixed under TDD; see the Verify-41 wave section).
2. ~~Rework the 3 refuted claims~~ **DONE** (resolved; V cells corrected to `✓`).
3. **`checks`/`cooling`/`sanitizer`/`temp-log` are C5-DELETE-BLOCKED** (owner decision 2026-07-07): do NOT delete them in any C5 wave until a native `sync_feed` producer exists or the block is explicitly lifted after confirming single-device deployment (see Verify-41 wave → Defer/ratify).
4. Triage the 9 low-confidence rows.
5. Confirm every **edge-retained** row appears in `lariat-native-edge-blockers.md` (the whole surviving web surface).
