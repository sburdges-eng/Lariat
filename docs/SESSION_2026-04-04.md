# Session Summary — 2026-04-04

## Completed Work

### Claude
- **Master Workbook update** — `Lariat_Unified_Workbook_2026-04-04.xlsx`:
  - Appended 13 Navratil BEO invoice rows + 12 prep/plating rows
  - Replaced 5-ingredient Pork Chop Marinade with 14-ingredient Southwestern Rye version (9 rows inserted, 72 ArrayFormulas regenerated)
- **CSV sync** — `recipe_book.csv` updated with new recipe name + 4-step procedure
- **Bug fixes** in Gemini's code:
  - `pages/8_Labor.py` — missing `Shift, ComplianceEngine` import
  - `libs/invoice_processor.py` — fuzzy match case mismatch + wrong dict key
  - `pages/14_KDS.py` — HTML-escaped star ticker to prevent DOM injection
- **Ingredient map** — added `worcestershire` → `Sauce Worcestershire` (Sysco SKU 7126651, $38.85/case 4gal)
- **Cleanup** — removed `operations_coverage.md` (superseded by `docs/ARCHITECTURE.md`), fixed stale path in `AGENTS.md`

### Gemini
- **New libs**: `recipe_parser.py` (shared recipe flattener), `labor_compliance.py` (CO COMPS #39), `invoice_processor.py` (SKU/fuzzy mapping)
- **New data**: `menus/beo_recipe_map.csv`, `menus/menu_station_map.csv`, `catering/events_log.csv` (full 12-col schema)
- **New scripts**: `beo_order_pull.py`, `inventory_forecast.py`
- **UI**: KDS page (14), Labor rewrite (8), Catering prep planner (7)
- **Docs**: `docs/ARCHITECTURE.md`

## Outstanding Issues

### P0 — Worcestershire pricing pipeline
Ingredient map entry added but `2026-03-29_merged_prices.csv` needs a re-run of `run_costing_spine.py` to pick up the Sysco line list price. Until then, `bom_cost.py` will still flag 1 unpriced ingredient.

### P1 — BEO recipe map data quality
`menus/beo_recipe_map.csv` line 5 maps `Baked Ziti` → `Green Chile` (wrong dish). Several items (Mac Balls, Mini Rellenos, Caprese Skewers, Churros, Philo Bites) map to recipe names that don't exist in `recipe_book.csv` — they silently drop from the order pull. Fine if they're ordered whole from Sysco, but the pull undercounts.

### P2 — KDS interactivity
KDS is read-only. Needs "bump" functionality to write task completion back to `workbook/working/prep_log.csv`.

### P3 — Labor shift-level data
Compliance engine mocks start/end times from weekly totals. Needs shift-level punch log parser for accurate 12hr OT and split-shift detection.

### P4 — Recipe nesting
`recipe_parser.py` flattens single-level recipes. Sub-recipes (e.g. "Lariat Rub" inside "Queso Sauce") need recursive expansion for accurate BOM costing.

## Files Modified This Session
```
libs/invoice_processor.py        (Gemini created, Claude fixed)
libs/labor_compliance.py         (Gemini created)
libs/recipe_parser.py            (Gemini created)
menus/beo_recipe_map.csv         (Gemini created)
menus/menu_station_map.csv       (Gemini created)
catering/events_log.csv          (Gemini created/updated)
pages/7_Catering.py              (Gemini modified)
pages/8_Labor.py                 (Gemini modified, Claude fixed)
pages/14_KDS.py                  (Gemini modified, Claude fixed)
scripts/beo_order_pull.py        (Gemini created/modified)
scripts/inventory_forecast.py    (Gemini created/modified)
scripts/update_workbook_navratil.py  (Claude created — one-shot)
workbook/data/recipe_book.csv    (Claude updated)
workbook/data/beo_invoices.csv   (pre-existing, verified)
workbook/data/beo_prep.csv       (pre-existing, verified)
costing/ingredient_vendor_map.csv (Claude updated)
docs/ARCHITECTURE.md             (Gemini created)
AGENTS.md                        (Claude fixed path)
```
