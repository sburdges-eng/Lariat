# Python test-time deps — install once with:  bash scripts/install-python-deps.sh
openpyxl>=3.1
xlrd>=2.0
pandas>=3.0
pytest>=8.0
# Imported unconditionally by tests/python via scripts/ (Sysco invoice-PDF
# ingest, Data Pack Wikibooks normalizer, Sysco order-email parser) — pytest
# collection fails without them.
lxml>=5.0
pdfplumber>=0.11
beautifulsoup4>=4.12
