# tasks.yaml — orchestrator manifest
#
# Phase 3.5 wave: COMPLETE (2026-05-14).
# Phase 4 wave:   COMPLETE (2026-05-14, same session).
#
# Phase 4 shipped (commit refs in ORCHESTRATOR_STATUS.md):
#   - #17 sync apply scheduler                                    (f15de27)
#   - #18 shared LARIAT_DATA_DIR resolver                         (ea2f8bd)
#   - #19 operator diagnostic CLI for the sync stack              (491aaf1)
#
# Future work (NOT in this manifest — captured here for planning):
#
#   1. DONE — /management/cloud-bridge operator UI form shipped
#      (CloudBridgeBoard.jsx round-trips cloudBridgeUrl/cloudBridgeSecret).
#
#   2. DONE — appendOp is wired into the regulated source-write paths
#      (checks, sanitizer, receiving, temp-log, cooling, …) with
#      lib/localIdentity.ts supplying sourceHost + sourceStartedAt.
#
#   3. Family-3 LWW applier (recipes, dish_components, entities_*)
#      Deferred per docs/multi-instance-sync.md design.
#
#   4. Ed25519 migration for cloud-bridge auth (Item 13)
#      Out of scope until the cloud peer's verifier ships.
#
# 2026-06-10: roadmap Tier-3 rows 3.2-3.7 implemented (uncommitted, pending
# review) on feat/tier3-no-config-features — see docs/PROJECT_ROADMAP.md.
#
# Read:   .claude/agents/coordinator.md  (slash-command entrypoint)
# Status: ORCHESTRATOR_STATUS.md

tasks: []
