// Placeholder so `tsc -p desktop` has at least one input until T3–T8 add the
// real entry points (main.ts, supervisor.ts, server-entry.cjs, paths.ts,
// settings.ts, preload.ts). This file should be deleted when desktop/main.ts
// lands in T7.
export {};
